# Phase 16 A TAA — slice audit

Commit: 47b1374
Date: 2026-05-17
Auditor: independent general-purpose agent (no prior knowledge of the implementation conversation)

## Scope

Files read independently against the binding §3.A sub-rubric:

* `backend/app/services/reporting/_schema_versions.py` — `TRUST_SCORE_TIMELINE_SCHEMA_VERSION` bump 1.1.0 → 1.2.0 with documented bump-history docstring (lines 263-289) citing "Phase 16 A · 2026-05-17", the additive `cumulative_drift_attribution` field, the 0/1/2-point degenerate-case semantics, the back-compat statement, and Phase 15 retro §3 closure.
* `backend/app/services/reporting/trust_score_timeline.py` — `TrustScoreTimeline` dataclass extended with `cumulative_drift_attribution: object = None` (lines 91-101); builder computes `cumulative_drift` via the Phase 15 C `compute_drift_attribution` SSOT helper at lines 152-158 (`points[0]` / `points[-1]`), skipping computation when `len(points) < 2`; JSON serializer renders `null` when `None`, otherwise `render_drift_attribution_dict(...)` (lines 380-384).
* `.planning/methodology/trust_score_drift_attribution.md` — new "Cumulative drift attribution (Phase 16 A · 2026-05-17)" section at the bottom (lines 75-94) documenting per-pair vs cumulative distinction, the 20 → 10 → 20 convergence-recovery example, all four degenerate cases (0/1/2/3+ point timelines), and Phase 15 retro §3 closure note.
* `tests/test_phase16_cumulative_drift_attribution.py` (NEW, 11 tests) — pins M:-1 schema version, T:-3 leak-arc cumulative `-100.0` boundary, T:-4 0/1/2-point degenerate cases, T:-3 canonical case sub-floor (dominant_axis None + NaN dominant_delta_pct), C:-1 Tier 1 trio + 1.2.0 envelope render, C:-8 9-token forbidden grep on the methodology doc, back-compat 1.1.0-reader smoke.
* `tests/test_phase15_trust_score_drift_attribution.py` — schema-pin renamed `test_timeline_schema_at_1_2_0` asserting `== "1.2.0"` (line 245-250). Alerts pin still at 1.1.0 (correct; that envelope's separate schema version is unchanged).
* `tests/test_phase15_journey_explicit_dynamics_drift_triage.py` — Journey 1 step 3 assertion bumped `1.1.0` → `1.2.0` at line 420 for the `/api/v1/trust-score-timeline/` route; line 383 for `/api/v1/trust-score-alerts/` correctly remains at 1.1.0.

Verification ran from `/Users/Zhuanz/20260408 AI StructureAnalysis`.

## Verification log

* `uv run pytest tests/test_phase16_cumulative_drift_attribution.py -v --no-header` → **11 passed in 0.47s**, all 11 slice-A tests green.
* `uv run pytest tests/ -q --no-header` → **2468 passed, 7 skipped in 27.35s** — matches the blueprint-expected count exactly. No flaky or skipped slice-A tests; no real-solver / real-LLM invocations triggered.
* Inspected lines 152-158 of `trust_score_timeline.py` and confirmed the cumulative computation invokes `compute_drift_attribution(_axes_dict(points[0]), _axes_dict(points[-1]), from_snapshot=points[0].snapshot_label, to_snapshot=points[-1].snapshot_label)`. No inline percentage math anywhere in the cumulative path.
* Inspected line 127's deferred import of `compute_drift_attribution` (Phase 15 C SSOT) and line 359's deferred import of `render_drift_attribution_dict`. Both helpers come from the SSOT module `trust_score_drift_attribution` — no duplicate inline math.
* Inspected `_timeline_to_dict` (lines 358-385) and confirmed `cumulative_drift_attribution` renders as `null` (Python `None`) when the field is `None`, otherwise as a `render_drift_attribution_dict` payload.
* Manual forbidden-token sweep on the methodology doc: 0 hits across the 9-token forbidden list. The new "Cumulative drift attribution (Phase 16 A)" paragraph is grep-clean.
* Manual forbidden-token sweep on `trust_score_timeline.py`: the only hits are inside the `_assert_no_overclaim` runtime-guard tuple (a guard against rendered JSON containing forbidden claims, NOT a positive claim) and the documented "no `<claim>`" negation in the module docstring. Both are intended patterns; the guard walks `_timeline_to_dict(timeline)` not the module source, so it can't trip itself.
* Manual back-compat trace: a hypothetical pre-1.2.0 reader walks `schema_version` / `points` / `inter_snapshot_drift_attribution` / `claim_tier` / `claim_boundary` / `claim_impact` — all six 1.1.0-era fields are present in the rendered envelope at the top level with their pre-1.2.0 shape preserved. The new `cumulative_drift_attribution` field is co-located at the top level and ignorable by any reader that doesn't know about it. `test_back_compat_1_1_0_reader_ignores_cumulative_field` formally pins this.

## Adversarial probes

1. **Probe — points[-2] off-by-one.** Modified `trust_score_timeline.py` line 154-157 to use `points[-2]` instead of `points[-1]`. Re-ran slice-A tests: **2 failures** (`test_two_point_timeline_cumulative_equals_single_pair`: cumulative was identifying snap-1 vs snap-1 with zero delta instead of snap-1 vs snap-2; `test_leak_arc_cumulative_dominant_energy_axis_minus_100_exact`: cumulative `to_snapshot == "2026-05-17T120000Z"` (snap-2) instead of `"2026-05-17T140000Z"` (snap-3)). The boundary test catches off-by-one cleanly. **Reverted to `points[-1]`.**
2. **Probe — PATCH instead of MINOR.** Modified `_schema_versions.py` `TRUST_SCORE_TIMELINE_SCHEMA_VERSION` from `"1.2.0"` to `"1.1.1"`. Re-ran the two schema-pin tests: **both failed** (`test_timeline_schema_version_at_1_2_0` + `test_rendered_timeline_carries_cumulative_field_at_1_2_0`). Pin enforcement is real. **Reverted to `"1.2.0"`.**
3. **Probe — SSOT helper grep (M:-2 anti-gaming).** Grep for `delta_pct` / `/ 100` / `* 100` / `percentage` in `trust_score_timeline.py`: percentage math hits are all in the pre-existing weighted-point scoring (`_completeness_weighted` / `_convergence_weighted` / `_energy_weighted` / `_reproducibility_weighted`, lines 229-320, Phase 6 D code unrelated to drift attribution). The cumulative path delegates entirely to `compute_drift_attribution(...)`. M:-2 verified.
4. **Probe — 0/1-point graceful degrade.** Ran `test_zero_point_timeline_cumulative_is_none` + `test_one_point_timeline_cumulative_is_none` against the unmodified source: both PASS. The `if len(points) >= 2:` guard (line 152) correctly skips the cumulative computation when fewer than 2 points exist; `cumulative_drift_attribution` remains `None`.
5. **Probe — from/to label off-by-one.** Inspected line 156-157 directly: `from_snapshot=points[0].snapshot_label, to_snapshot=points[-1].snapshot_label`. Confirmed the cumulative entry's labels span first→last, not (points[-2], points[-1]) or any other off-by-one variant. `test_leak_arc_cumulative_dominant_energy_axis_minus_100_exact` lines 318-319 pin these labels precisely.
6. **Probe — forbidden-token regression scope.** The methodology doc's new Phase 16 A section was hand-grepped for all 9 forbidden tokens with prefix-aware `not <claim>` / `no <claim>` allowance: 0 hits. The shipped `test_methodology_doc_cumulative_section_passes_forbidden_grep` test also passes.

**All probe modifications reverted before completing this report.** Final state matches commit 47b1374 verbatim.

## Per-axis scoring (63 total)

| Axis | Cap | Score | Rationale |
|---|---|---|---|
| M | 12 | 12/12 | M:-1 schema MINOR bump 1.1.0 → 1.2.0 with full bump-history docstring (lines 280-288 of `_schema_versions.py`) explicitly citing "Phase 16 A · 2026-05-17", the additive `cumulative_drift_attribution` field, the degenerate-case semantics, the back-compat statement, and Phase 15 retro §3 closure. M:-2 cumulative computation is a SINGLE invocation of the Phase 15 C `compute_drift_attribution` SSOT (lines 152-158); zero inline percentage math (Probe 3 verified). M:-3 methodology doc grows a new "Cumulative drift attribution (Phase 16 A)" section (lines 75-94) explicitly contrasting per-pair ("where in the arc did each axis change?") vs cumulative ("what is the NET change between first and last observation, regardless of the path taken?") and giving the 20 → 10 → 20 recovery example to motivate why both views are useful. |
| T | 15 | 15/15 | T:-3 boundary pin `test_leak_arc_cumulative_dominant_energy_axis_minus_100_exact` asserts `dominant_axis == "energy_audit"` AND `dominant_delta_pct == -100.0` AND `from_snapshot == SNAP_1_LABEL` AND `to_snapshot == SNAP_3_LABEL` (i.e., spans the full leak arc, not just the snap-1→snap-2 transition). T:-4 0-point → None (`test_zero_point_timeline_cumulative_is_none`), 1-point → None (`test_one_point_timeline_cumulative_is_none`), 2-point → equals the single per-pair entry's percentages + dominant_axis + labels (`test_two_point_timeline_cumulative_equals_single_pair`). Schema-version pin `test_timeline_schema_version_at_1_2_0` + the rendered-envelope pin `test_rendered_timeline_carries_cumulative_field_at_1_2_0` both target 1.2.0. Tier 1 trio + claim-impact assertion present on the rendered envelope. Also pins `test_canonical_case_cumulative_uniform_sub_floor` (canonical case stays sub-floor uniform → dominant_axis None + NaN dominant_delta_pct + every axis pct == 0.0). 11 tests total — exceeds the "~10 tests" blueprint target. |
| C | 12 | 12/12 | C:-1 Tier 1 disclaimer trio (`claim_tier == "Tier 1 engineering candidate"` + `claim_boundary` contains `not_signed_validation` + `not_benchmark_agreement`) preserved on the timeline envelope at schema 1.2.0; pinned by `test_rendered_timeline_carries_cumulative_field_at_1_2_0`. C:-8 forbidden-token grep clean on the methodology doc; pinned by `test_methodology_doc_cumulative_section_passes_forbidden_grep` walking the same 9-token list as Phase 15 with prefix-aware `not <claim>` / `no <claim>` allowance. C:-3 "what this surface does NOT do" section preserved verbatim from Phase 15 C; the new Phase 16 A section adds its own "Degenerate cases" enumeration and explicit "Both views are useful; surface both" framing — per-pair AND cumulative co-exist (additive, not replacement). |
| A | 8 | 8/8 | A:-1 schema MINOR (additive): the new field defaults to `None` on the dataclass (line 91); JSON serializer emits `null` when `None` (lines 380-384); the 1.1.0-era fields are all preserved unchanged in their pre-1.2.0 shape (verified by `test_back_compat_1_1_0_reader_ignores_cumulative_field`). A:-3 canonical-case sub-floor: pinned by `test_canonical_case_cumulative_uniform_sub_floor` asserting cumulative dominant_axis is None + NaN dominant_delta_pct + every per-axis pct == 0.0, which preserves the sub-floor uniform-drift posture the canonical case carries throughout the cohort. |
| E | 8 | 8/8 | E:-1 cumulative computation rationale documented at length in the methodology doc (the "Per-pair answers..." / "Cumulative answers..." framing + the 20 → 10 → 20 recovery example explicitly showing why cumulative magnitude can be SMALLER than the worst per-pair entry). E:-2 monotonic-axis invariant pinned by `test_leak_arc_cumulative_consistent_with_per_pair_when_monotonic`: when an axis drops in a single transition and STAYS at the floor (energy 15 → 0 → 0), the cumulative `dominant_delta_pct == per-pair[0]'s dominant_delta_pct == -100.0`. E:-3 NaN → null preserved via the existing `render_drift_attribution_dict` helper (Phase 15 C SSOT); pinned indirectly by `test_canonical_case_cumulative_uniform_sub_floor` (NaN dominant_delta_pct on canonical) + the rendered-envelope test. |
| V | 8 | 8/8 | V:-1 closes Phase 15 retrospective §3 (drift attribution rendered on more envelopes than alerts + timeline-per-pair) — explicitly cited in the bump-history docstring AND the methodology doc's closing line. V:-2 feeds Phase 16 D Journey 2 by providing the cumulative-vs-consecutive comparison surface the journey-cumulative-vs-consecutive-drift test will exercise (per the blueprint § Slice D). V:-3 forbidden-token grep + Tier 1 trio + HF1 path-guard discipline all preserved — slice A makes no signed-registry edits, no `^GS-\d{3}$` route changes, no `golden_samples/` writes outside ephemeral tmp_path fixtures. The 2468/2475 full sweep is green, including the cross-route signed-registry refusal pins from Phase 14 A. |
| **Total** | **63** | **63/63** | All axes at cap; every binding floor exceeded. |

## Verdict

**APPROVE.**

Slice A scores **63/63**, every axis at full credit (M 12/12, T 15/15, C 12/12, A 8/8, E 8/8, V 8/8). All binding floors (M ≥10, T ≥10, C ≥10, A ≥6, E ≥7, V ≥7) cleared with margin. The stop condition ≥60/63 AND every axis ≥ binding-floor is met decisively. The full 2468-test sweep stays green at the blueprint-expected count exactly. Six adversarial probes all behaved correctly (off-by-one caught, schema-pin caught, SSOT helper enforced, 0/1-point graceful degrade verified, from/to labels span first→last, forbidden-token grep clean on doc + module). All probe modifications were reverted before this report; final tree state matches commit 47b1374 verbatim.

## Open observations (non-blocking)

* The `cumulative_drift_attribution` field on the `TrustScoreTimeline` dataclass is typed `object = None` rather than `DriftAttribution | None`. This mirrors the existing `inter_snapshot_drift_attribution: tuple = ()` typing pattern in the same dataclass (Phase 15 C also chose loose typing, presumably to avoid a heavy import at module load). The runtime behavior is correct (tests verify `isinstance(cumulative, DriftAttribution)` at line 288 of the new test file), but a future janitorial pass could tighten the type to `DriftAttribution | None` once the import-cost / circularity tradeoff is re-evaluated. Not a blocker — and the deferred-import pattern at lines 127, 359 explicitly cites cost-deferral as the rationale.
* `_assert_no_overclaim` (lines 388-398) walks `_timeline_to_dict(timeline)` which now includes the rendered `cumulative_drift_attribution` payload — the existing runtime guard transparently extends to the new field with no new code path required. This is the right design and an implicit T:-3 reinforcement; worth highlighting in the Phase 16 retrospective when it lands at slice E.
* The new methodology section explicitly enumerates all four degenerate cases (0/1/2/3+ point) — matches the test-pinned cases exactly. Excellent documentation/test alignment.
* The slice-A test file's docstring (lines 1-28) cross-references the binding rubric's M/T/C anti-gaming guard codes (M:-1, M:-2, T:-3, T:-4, C:-1, C:-8) so future auditors can map test → rubric clause without spelunking. Continues the Phase 11+ practice of self-documenting test surfaces.
