# Phase 16 B TAA — slice audit

Commit: 0293fea
Date: 2026-05-17
Auditor: independent general-purpose agent (no prior knowledge of the implementation conversation)

## Scope

Files read independently against the binding §3.B sub-rubric:

* `backend/app/services/reporting/cohort_drift_attribution.py` (NEW · 266 LOC) — exposes `COHORT_DOMINANT_AXIS_FLOOR_PCT: float = 5.0` (module-level, typed, named SSOT — lines 62-74), `CohortDriftAttribution` frozen dataclass (lines 85-106) with `from_snapshot / to_snapshot / per_case_drift_attribution / cohort_dominant_axis / cohort_max_abs_delta_pct / dominant_case_id`, `compute_cohort_drift_attribution(*, repo_root, dominant_floor_pct=5.0)` (lines 150-240) walking signed-registry-filtered `golden_samples/*-candidate/` and delegating per-case math to the Phase 15 C `compute_drift_attribution` SSOT (lines 200-215), and `render_cohort_drift_attribution_dict` (lines 243-266) with NaN→null + None pass-through.
* `backend/app/services/reporting/cohort_anomalies.py` — wires the new field on `CohortAnomaliesReport` at line 110 (`cohort_drift_attribution: object = None`); builder invokes `compute_cohort_drift_attribution(repo_root=repo_root)` via deferred import at lines 199-201 (avoiding the trust_score_timeline import cycle); JSON serializer renders `cohort_drift_attribution` via the SSOT helper at lines 250-252. Z-score view (lines 170-194) preserved entirely (additive, parallel). `_assert_no_overclaim` (lines 264-279) walks the rendered envelope unchanged.
* `backend/app/services/reporting/_schema_versions.py` — `COHORT_ANOMALIES_SCHEMA_VERSION` bumped `"1.0.0"` → `"1.1.0"` (line 330) with a complete bump-history docstring (lines 343-356) citing "Phase 16 B · 2026-05-17", the additive `cohort_drift_attribution` field, the pre-1.1.0 back-compat statement, the z-score preservation, the null-when-fewer-than-2-snapshots semantics, and Phase 15 retro §4 + §6 closure.
* `.planning/methodology/cohort_drift_attribution.md` (NEW · 73 lines) — documents the per-case → cohort aggregation, the 5.0% floor's independent tunability vs the per-case floor, the bump policy, the worked example payload (5-case cohort with leak case dominant), and an explicit "What this surface does NOT do" section enumerating (a) no regression root-cause, (b) does not replace z-score view, (c) does not certify Tier 2, (d) does not classify step-change-vs-slow-drift.
* `tests/test_phase16_cohort_drift_attribution.py` (NEW · 15 tests · 485 LOC) — M:-1 schema pin, M:-2 floor=5.0 + type pin, A:-2 non-positive-floor raises + fewer-than-2-snapshots returns None, T:-3 boundary pin (`cohort_dominant_axis=="energy_audit"` + `dominant_case_id==LEAK_CASE_ID` + `cohort_max_abs_delta_pct==100.0` exact), per-case 5-member carry pin, leak-case per-case dominant pin, healthy-cases-have-no-dominant pin, floor-tuning suppression pin (`dominant_floor_pct=101.0` → None + NaN), live ASGI envelope pin (Tier 1 trio + z-score view + new field all present at 1.1.0), null-when-no-snapshots envelope pin, render helper None pass-through + NaN→null, C:-8 9-token forbidden-grep with prefix-aware allowance, back-compat 1.0.0 reader smoke.

Verification ran from `/Users/Zhuanz/20260408 AI StructureAnalysis`.

## Verification log

* `uv run pytest tests/test_phase16_cohort_drift_attribution.py -v --no-header` → **15 passed in 0.54s**. All 15 slice-B tests green; no skips.
* `uv run pytest tests/ -q --no-header` → **2483 passed, 7 skipped in 24.57s** — matches the blueprint-expected count exactly (baseline 2468 + 15 new = 2483). No flakes, no real-solver / real-LLM invocations.
* Inspected lines 199-201 of `cohort_anomalies.py` and confirmed the cohort drift computation is a SINGLE deferred-import + SINGLE invocation of `compute_cohort_drift_attribution(repo_root=repo_root)`. Grep on `cohort_anomalies.py` for `delta_pct` / `/ 100` / `* 100` returned **zero hits** — M:-2 helper-not-inline confirmed; no inline per-case percentage math anywhere in the consumer.
* Inspected lines 200-215 of `cohort_drift_attribution.py` and confirmed the per-case math delegates entirely to `compute_drift_attribution(...)` (Phase 15 C SSOT) with the cohort-wide latest pair labels — no parallel implementation of percentage math at cohort scope. The cohort-level aggregation (lines 218-223) only walks `att.per_axis_delta_pct.items()` to find the (case, axis) argmax.
* Inspected lines 112-126 of `cohort_drift_attribution.py` (`_discover_latest_snapshot_pair`): `labels = sorted(...)` ascending then `return labels[-2], labels[-1]` returns chronologically (prev, latest). Pinned indirectly by `test_cohort_dominant_axis_is_energy_audit_on_leak_case` asserting `from_snapshot == SNAP_2_LABEL` AND `to_snapshot == SNAP_3_LABEL` (the latter being snap-3, chronologically last).
* Inspected lines 129-147 (`_discover_cohort_cases`): mirrors the `cohort_anomalies.py` triple filter — `endswith("-candidate")`, NOT `_SIGNED_REGISTRY_RE.fullmatch(name)` (the bare `GS-\d{3}` shape per HF1.7a), AND `_CANDIDATE_DIR_RE.fullmatch(name)` (the `[A-Za-z0-9_-]+-candidate` allow-shape). Phase 14 A cross-route signed-registry SSOT not bypassed.
* Manual forbidden-token sweep on `cohort_drift_attribution.py`: 8 hits, ALL inside the "Forbidden wording" module docstring block (lines 38-42), each prefixed with `no ` (negated form). The test `test_no_forbidden_positive_claims_in_new_module` walks the same allow-form.
* Manual forbidden-token sweep on `.planning/methodology/cohort_drift_attribution.md`: 0 hits. Grep-clean.
* Manual back-compat trace: the rendered 1.1.0 envelope carries all 8 1.0.0-era fields (`schema_version` / `generated_at_utc` / `claim_tier` / `claim_boundary` / `cohort_count` / `anomaly_count` / `anomalies` / `claim_impact`) at the top level with their pre-1.1.0 shape preserved. The new `cohort_drift_attribution` field is co-located at the top level and ignorable by any reader that doesn't know about it. `test_back_compat_1_0_0_reader_still_parses` formally pins this.
* `git status` reveals two unrelated working-tree modifications on `_schema_versions.py` + `signoff_record.py` introducing `SIGNOFF_RECORD_SCHEMA_VERSION` 1.0.0 → 1.1.0 with Phase 16 C bump-history. These are slice-C work-in-progress, **NOT part of commit `0293fea`** and **OUT OF SCOPE** for this audit. The slice-B-committed `_schema_versions.py` delta is solely the COHORT_ANOMALIES bump.

## Adversarial probes

1. **Probe — `>` → `>=` aggregation comparator** (M:-2 anti-gaming sanity). Modified line 220 of `cohort_drift_attribution.py` (`if abs_delta > cohort_max_abs:` → `if abs_delta >= cohort_max_abs:`). Re-ran slice-B tests: **all 15 still PASS**. As expected — the boundary case has a single dominant outlier at 100% and any tie behavior is degenerate (`abs_delta == cohort_max_abs == 0.0` would overwrite to the lexicographically-last healthy case, but the sub-floor guard at line 226 would zero out `cohort_dominant_axis` to None anyway). The mutation is benign in slice-B test data. **NOT a meaningful regression**; the `>` is the principled choice (deterministic first-seen winner) but tests do not pin it. Worth a non-blocking observation. **Reverted to `>` immediately.**
2. **Probe — graceful degrade 0/1 snapshots.** Ran `test_compute_returns_none_when_fewer_than_two_snapshots` against unmodified source: PASSES. `_discover_latest_snapshot_pair` returns None when `len(labels) < 2`, and `compute_cohort_drift_attribution` propagates that None at line 176-177. The function does NOT raise; A:-2 graceful-degrade verified.
3. **Probe — signed-registry filter on `_discover_cohort_cases`.** Read lines 129-147. Three guards stack: suffix `-candidate`, NOT signed-registry shape `GS-\d{3}`, AND allow-shape `[A-Za-z0-9_-]+-candidate`. A `golden_samples/GS-001/` directory would be skipped by guard 1 (suffix); `golden_samples/GS-001-candidate/` would pass guard 1 but be subject to guard 2 — but `GS-001-candidate` does not fullmatch `^GS-\d{3}$` so it would slip through guard 2 too. **This is consistent with `cohort_anomalies.py` lines 147-152 (identical triple filter)**: both modules treat the bare `GS-\d{3}` form as the signed-registry shape, NOT `GS-\d{3}-anything`. Phase 14 A SSOT semantic preserved. No bypass.
4. **Probe — latest-pair discovery direction.** Read lines 119-126: `sorted(child.name for child in root.iterdir() if SNAPSHOT_LABEL_RE.fullmatch(...))` gives ISO labels sorted ascending; `return labels[-2], labels[-1]` therefore returns (prev, latest) in chronological order, not reversed. The boundary test pins `from_snapshot == SNAP_2_LABEL` AND `to_snapshot == SNAP_3_LABEL` (snap-3 is chronologically newest by ISO timestamp).
5. **Probe — `golden_samples/cylinder-pv-candidate/` on disk.** `ls golden_samples/cylinder-pv-candidate/` returns `data/` + `expected_results.json/`. Present, not pulled-via-subprocess — version-controlled in the original cohort (the test fixture `seeded_5_case_arc` `shutil.copytree`s it into `tmp_path` along with the 4 other cohort members). Cohort discovery is real, not synthetic.
6. **Probe — floor-tuning suppression.** `test_below_floor_cohort_has_no_dominant` calls `compute_cohort_drift_attribution(repo_root=seeded_5_case_arc, dominant_floor_pct=101.0)` and asserts `cohort_dominant_axis is None` + `dominant_case_id is None` + `math.isnan(cohort_max_abs_delta_pct)`. PASSES. The sub-floor sentinel (line 229: `cohort_max_abs_emit = math.nan`) flows through `render_cohort_drift_attribution_dict` lines 260-264 (NaN→null) correctly. Pinned by `test_render_helper_nan_becomes_null` too.
7. **Probe — forbidden-token regression scope.** All 8 hits in `cohort_drift_attribution.py` are confined to the docstring's "Forbidden wording" enumeration (lines 38-42), each preceded by `no ` in the same line. The methodology doc has zero hits across all 9 tokens. The shipped `test_no_forbidden_positive_claims_in_new_module` walks both files with prefix-aware `not <claim>` / `no <claim>` allowance.
8. **Probe — back-compat 1.0.0 reader.** `test_back_compat_1_0_0_reader_still_parses` PASSES — the rendered 1.1.0 envelope still carries all 8 1.0.0-era top-level fields (`schema_version` / `generated_at_utc` / `claim_tier` / `claim_boundary` / `cohort_count` / `anomaly_count` / `anomalies` / `claim_impact`). The new `cohort_drift_attribution` field is additive and ignorable. Pre-1.1.0 readers can parse 1.1.0 envelopes without modification.

**All probe modifications reverted before completing this report.** `git diff` on the 4 slice-B-scoped files returns empty. Final state matches commit 0293fea verbatim.

## Per-axis scoring (63 total)

| Axis | Cap | Score | Rationale |
|---|---|---|---|
| M | 12 | 12/12 | M:-1 schema MINOR bump 1.0.0 → 1.1.0 with full bump-history docstring (`_schema_versions.py` lines 343-356) citing "Phase 16 B · 2026-05-17", the additive `cohort_drift_attribution` field, the pre-1.1.0 back-compat statement, the z-score preservation, the null-when-no-snapshots semantics, and Phase 15 retro §4 + §6 closure. M:-2 `COHORT_DOMINANT_AXIS_FLOOR_PCT: float = 5.0` is module-level, typed, named SSOT (lines 62-74); `CohortDriftAttribution` is a frozen dataclass (lines 85-106); consumer `cohort_anomalies.py` imports `compute_cohort_drift_attribution` (line 199) and invokes it as a SINGLE call (line 201) with no inline percentage math (Probe — grep returned zero `delta_pct` / `/ 100` / `* 100` hits in `cohort_anomalies.py`). The per-case math itself delegates to the Phase 15 C `compute_drift_attribution` SSOT (lines 200-215), not a parallel implementation. M:-3 floor value documented in methodology doc with explicit bump policy (lines 36-44) — both bump-down (tighter cohort sensitivity) AND bump-up (false-positive budget) paths described with retrospective-entry audit-trail requirement; slipping-to-mask-regression named as anti-pattern. |
| T | 15 | 15/15 | T:-3 boundary pin `test_cohort_dominant_axis_is_energy_audit_on_leak_case` asserts `cohort_dominant_axis == "energy_audit"` AND `dominant_case_id == LEAK_CASE_ID` AND `cohort_max_abs_delta_pct == 100.0` exactly, against a seeded 5-case arc where the leak case's energy axis collapses 15→0 between snap-2 and snap-3. Floor-tuning behavior pinned by `test_below_floor_cohort_has_no_dominant`: bumping `dominant_floor_pct` to 101.0 suppresses the cohort_dominant_axis (None) + dominant_case_id (None) + magnitude (NaN). All 5 cohort members carried by `test_cohort_per_case_carries_all_5_members`. Leak case's per-case attribution within the cohort summary pinned by `test_cohort_leak_case_per_case_dominant_energy_minus_100_exact` (`dominant_axis == "energy_audit"` + `dominant_delta_pct == -100.0` + `per_axis_delta_pct["energy_audit"] == -100.0`). Healthy cases' sub-floor uniform-drift posture pinned by `test_cohort_healthy_cases_have_no_dominant_axis`. Live ASGI integration pin: `test_cohort_anomalies_envelope_carries_cohort_drift_field` walks the rendered envelope and confirms (a) schema_version 1.1.0, (b) Tier 1 trio (`claim_tier == "Tier 1 engineering candidate"` + `not_signed_validation` + `not_benchmark_agreement` in `claim_boundary`), (c) z-score `anomalies` field present, (d) new `cohort_drift_attribution` present with the dominant pair, (e) per-case entries each carry `case_id` + `per_axis_delta_pct`. 15 tests total — exceeds the "~15 tests" blueprint target. |
| C | 12 | 12/12 | C:-1 Tier 1 disclaimer trio (`claim_tier` + `claim_boundary` containing `not_signed_validation` + `not_benchmark_agreement`) preserved on the cohort-anomalies envelope at 1.1.0; pinned by `test_cohort_anomalies_envelope_carries_cohort_drift_field` lines 354-356. The `_assert_no_overclaim` runtime guard in `cohort_anomalies.py` (lines 264-279) transparently extends to the new field's payload because it walks the full `_report_to_dict(report)` output. C:-8 forbidden-token grep clean: `test_no_forbidden_positive_claims_in_new_module` walks both `cohort_drift_attribution.py` and the methodology doc against the 9-token list with prefix-aware `no <claim>` / `not <claim>` allowance — PASSES. Methodology doc has zero hits across all 9 tokens; module file's 8 hits are confined to the "Forbidden wording" enumeration with `no ` prefix on each. C:-3 methodology doc carries an explicit "What this surface does NOT do" section (lines 46-51) enumerating (a) no regression root-cause analysis, (b) does NOT replace the z-score view (additive, parallel), (c) does NOT certify Tier 2, (d) does NOT classify step-change-vs-slow-drift. Z-score view explicitly preserved on the envelope (additive, not replacement) — line 49 of methodology + the line-170-194 z-score loop in `cohort_anomalies.py` untouched. |
| A | 8 | 8/8 | A:-2 defensive parser raises ValueError on non-positive floor (`compute_cohort_drift_attribution(..., dominant_floor_pct=0.0)` and `=-1.0` both raise; pinned by `test_compute_raises_on_non_positive_floor`). Returns None (NOT raises) when fewer than 2 snapshots exist (`_discover_latest_snapshot_pair` returns None → `compute_cohort_drift_attribution` returns None at line 176-177; pinned by `test_compute_returns_none_when_fewer_than_two_snapshots`). Graceful degrade also covers cases absent from one of the latest 2 snapshots (line 197-199: `if prev_point is None or curr_point is None: continue`) — case is silently skipped, no exception. Signed-registry filtering preserved: `_discover_cohort_cases` (lines 129-147) applies the identical triple filter as `cohort_anomalies.py` — bare `GS-\d{3}` excluded; Phase 14 A cross-route SSOT not bypassed. Live envelope ASGI test confirms `cohort_drift_attribution: null` when no snapshots (`test_cohort_anomalies_envelope_null_drift_when_no_snapshots`). |
| E | 8 | 8/8 | E:-1 SSOT 5%-floor rationale documented in methodology doc (lines 14-15: matches per-case floor for coherent reviewer cross-reading; lines 36-44: bump policy with audit-trail requirement; line 38: explicit "INDEPENDENT from the per-case floor" tunability statement). E:-2 cohort-wide snapshot-pair coherence: every case is compared on the SAME latest 2 snapshot labels (`pair = _discover_latest_snapshot_pair(repo_root)` resolves ONCE at line 175, then used for every case-iteration; lines 213-214 pass `from_snapshot=prev_label, to_snapshot=curr_label` to the Phase 15 C helper, anchoring the comparison). Cases absent from one of the two latest snapshots are silently skipped (line 197-199) — preserves cohort-wide pair coherence even with partial snapshot membership. E:-3 NaN → null JSON rendering: `cohort_max_abs_delta_pct` rendered as `null` when NaN (lines 260-264 of module); pinned by `test_render_helper_nan_becomes_null`. None pass-through pinned by `test_render_helper_passes_through_none` (the `att is None` early-return at line 250-251). Live envelope NaN→null sweep covered indirectly by `test_below_floor_cohort_has_no_dominant` flowing through `render_cohort_anomalies_json`. |
| V | 8 | 8/8 | V:-1 slice closes Phase 15 retrospective §4 (per-axis percentage delta on cohort-scoped surfaces) + §6 (cross-axis-comparable cohort signal) — explicitly cited in the bump-history docstring (`_schema_versions.py` lines 354-356) AND the methodology doc header (line 7) AND the commit message. V:-2 feeds Phase 16 D Journey 1 (drift audit trail) by providing the `cohort_dominant_axis == "energy_audit"` + `dominant_case_id == LEAK_CASE_ID` surface the journey test will exercise on the cohort-anomalies route (per the blueprint §3.D line 140). V:-3 forbidden-token + Tier 1 trio + HF1 path-guard discipline all preserved at envelope + module + methodology layers. Slice B makes no signed-registry edits, no `^GS-\d{3}$` route changes, no `golden_samples/` writes outside the test fixture's `tmp_path` + `shutil.copytree` from the version-controlled cohort. The 2483/2475 full sweep is green, including the cross-route signed-registry refusal pins from Phase 14 A and the Phase 15 C SSOT helper tests. |
| **Total** | **63** | **63/63** | All axes at cap; every binding floor exceeded with margin. |

## Verdict

**APPROVE.**

Slice B scores **63/63**, every axis at full credit (M 12/12, T 15/15, C 12/12, A 8/8, E 8/8, V 8/8). All binding floors (M ≥10, T ≥10, C ≥10, A ≥6, E ≥7, V ≥7) cleared with margin. The stop condition ≥60/63 AND every axis ≥ binding-floor is met decisively. The full 2483-test sweep stays green at the blueprint-expected count exactly (baseline 2468 + 15 new = 2483). Eight adversarial probes all behaved correctly (`>` vs `>=` non-load-bearing on this test data but principled choice intact, 0/1-snapshot graceful degrade verified, signed-registry filter triple-stacked, latest-pair direction chronologically-ascending, golden_samples cohort version-controlled, floor-tuning suppression pinned, forbidden-token grep clean, back-compat 1.0.0 reader pinned). All probe modifications were reverted before this report; final tree state of the 4 slice-B-scoped files matches commit 0293fea verbatim.

## Open observations (non-blocking)

* The `>` vs `>=` comparator at line 220 of `cohort_drift_attribution.py` is not pinned by any test (Probe 1). Both behaviors converge to the same result on the slice-B fixture because there is a single 100% outlier and ties on healthy 0.0 deltas are absorbed by the sub-floor guard. The `>` choice is principled (first-seen-winner determinism) but a future contributor could flip it without breaking tests. Worth a non-blocking pin in Phase 16 E retrospective: add a 2-case test where two cases share an identical maximum |delta_pct| on different axes and assert which one becomes `dominant_case_id`. Not a regression; just an underconstrained corner of the spec.
* The `cohort_drift_attribution` field on the `CohortAnomaliesReport` dataclass (line 110) is typed `object = None` rather than `CohortDriftAttribution | None`. This mirrors the Phase 16 A `cumulative_drift_attribution: object = None` typing in `TrustScoreTimeline` and is consistent with the deferred-import pattern (line 199, line 224) explicitly chosen to avoid the trust_score_timeline import cycle. Runtime behavior is correct; a future janitorial pass could tighten the typing once the cycle is broken.
* The `_assert_no_overclaim` runtime guard in `cohort_anomalies.py` (lines 264-279) walks `_report_to_dict(report)` which now includes the rendered `cohort_drift_attribution` payload — the existing runtime guard transparently extends to the new field with no new code path required. Same observation as Phase 16 A; this is a load-bearing design property and worth calling out in the Phase 16 retrospective.
* The slice-B test file's docstring (lines 1-27) cross-references the binding rubric's M/T/C/A anti-gaming guard codes (M:-1, M:-2, T:-3, A:-2, C:-1, C:-8) so future auditors can map test → rubric clause without spelunking. Continues the Phase 11+ practice.
* Slice C work-in-progress (`SIGNOFF_RECORD_SCHEMA_VERSION` 1.0.0 → 1.1.0 + `signoff_record.py` deltas) is uncommitted in the working tree at audit time. **OUT OF SCOPE for slice B**; the slice-B commit `0293fea` contains exactly the 5 expected in-scope files (cohort_drift_attribution.py NEW · cohort_anomalies.py wiring · _schema_versions.py bump · methodology doc NEW · test file NEW) plus the Phase 16 A.md audit archive. The slice-C work is correctly held back from the slice-B commit boundary and will receive its own per-slice TAA at slice-C closure.
* The methodology doc's worked-example payload (lines 18-32) shows the leak case at `dominant_delta_pct: -100.0` — sign-preserving (negative because energy collapsed). Matches the `per_axis_delta_pct["energy_audit"] == -100.0` invariant pinned by `test_cohort_leak_case_per_case_dominant_energy_minus_100_exact`. The cohort-level `cohort_max_abs_delta_pct: 100.0` is the ABSOLUTE value (no sign) — explicit naming convention (`max_abs`) makes this self-documenting. Coherent vs Phase 15 C's signed `dominant_delta_pct` convention; reviewers reading both surfaces should not be confused.
