# Phase 16 C TAA — slice audit

Commit: bbe3402
Date: 2026-05-17
Auditor: independent general-purpose agent (no prior knowledge of the implementation conversation)

## Scope

Files read independently against the binding §3.C sub-rubric:

* `backend/app/services/reporting/signoff_record.py` — `SignoffRecord` frozen dataclass extended at line 195 with `drift_attribution_at_signoff_time: object = None` plus a 10-line docstring (lines 196-205) citing "Phase 16 C", the AT-WRITE-TIME semantics, the A:-3 server-computed posture, and the schema 1.1.0 additive-field back-compat contract. `write_signoff_record` (lines 211-267) signature unchanged on the caller side — accepts `case_id / reviewer / verdict / notes / *, repo_root, now_utc` — and internally computes drift via a NEW private helper `_compute_drift_attribution_at_signoff_time(case_id, *, repo_root=repo_root)` (line 251-253) just before constructing the record at line 255-262. New helper at lines 270-289 imports `build_trust_score_timeline` lazily and returns `timeline.inter_snapshot_drift_attribution[-1]` (the snap-(N-1) → snap-N consecutive-pair entry — the LOAD-BEARING pair for "what was regressing at signoff time"); returns `None` when the inter-snapshot tuple is empty (graceful degrade for 0/1-snapshot cases). `read_signoff_history` (lines 295-335) extended at lines 330-332 to call a NEW private parser `_parse_drift_attribution(payload.get("drift_attribution_at_signoff_time"))` — `.get` (not `[]`) makes pre-1.1.0 records read as `None`. `_parse_drift_attribution` (lines 338-359) tolerates `None` / non-dict blobs (returns None) and reconstructs the `DriftAttribution` dataclass via lazy import from `trust_score_drift_attribution`, handling the NaN-rendered-as-null `dominant_delta_pct` case (`None` → `float("nan")`). `_record_to_dict` (lines 365-386) extended at lines 366-371 to render the drift via the Phase 15 C SSOT `render_drift_attribution_dict` helper with `None` pass-through (`drift_dict = render_drift_attribution_dict(drift) if drift is not None else None`). Envelope helpers (`build_signoff_history_report` lines 445-465, `render_signoff_history_json` line 468-469, `_assert_no_overclaim_in_report` lines 485-509) untouched in shape; the runtime audit at line 496 walks the full `_report_to_dict` haystack which now transparently includes the new field's rendered subdict.
* `backend/app/services/reporting/_schema_versions.py` — `SIGNOFF_RECORD_SCHEMA_VERSION` bumped `"1.0.0"` → `"1.1.0"` (line 464) with a complete bump-history docstring (lines 480-494) citing "Phase 16 C · 2026-05-17", the additive `drift_attribution_at_signoff_time` field, the explicit "SERVER-COMPUTED (NOT trusted from a client POST body — A:-3 guard)" posture clause, the null-when-fewer-than-2-snapshots semantics, the closure of the "WHAT regressed vs WHO judged it" audit-trail gap, and the pre-1.1.0 back-compat statement.
* `tests/test_phase16_signoff_drift_capture.py` (NEW · 13 tests · 619 LOC) — M:-1 schema pin, two graceful-degrade pins (0-snapshot + 1-snapshot → None), T:-3 boundary pin against the Phase 15 B 3-snapshot leak arc (snap-2 → snap-3 latest pair; energy axis 0 → 0 = 0%, completeness regression dominates), fresh-arc 2-snapshot 15→0 pin (`energy_audit` -100.0 exact + dominant_axis == "energy_audit" + dominant_delta_pct == -100.0 exact), A:-3 LOAD-BEARING `inspect.signature` pin (no kwarg accepted), JSON round-trip preservation pin (from/to/per_axis/dominant), 1.1.0 envelope carries new field + Tier 1 trio pin, back-compat pre-1.1.0 on-disk read pin (None drift), runtime envelope audit clean pin (NOT a source-file grep — explicit comment at lines 487-491 names why the module legitimately carries the forbidden-token data literals), signed-registry refusal preserved pin, None-drift renders as null pin, NaN dominant_delta_pct renders as null pin.

Verification ran from `/Users/Zhuanz/20260408 AI StructureAnalysis`.

## Verification log

* `uv run pytest tests/test_phase16_signoff_drift_capture.py -v --no-header` → **13 passed in 0.52s**. All 13 slice-C tests green; no skips, no flakes.
* `uv run pytest tests/ -q --no-header` → **2496 passed, 7 skipped in 37.09s** — matches the blueprint-expected count exactly (slice-B baseline 2483 + 13 new = 2496). No flakes, no real-solver / real-LLM invocations.
* Inspected `signoff_record.py` lines 270-289: `_compute_drift_attribution_at_signoff_time` is a 20-line thin shim — `build_trust_score_timeline(case_id, repo_root)` → `timeline.inter_snapshot_drift_attribution[-1]` with the 2-snapshot threshold guard. No inline percentage math, no parallel implementation of Phase 15 C's `compute_drift_attribution` SSOT. **M:-2 helper-not-inline confirmed.** The percentage math originates in `build_trust_score_timeline`'s `inter_snapshot_drift_attribution` field which itself delegates to `compute_drift_attribution` (Phase 15 C SSOT).
* `grep -n "compute_drift_attribution\|delta_pct\|/100\|\* 100" signoff_record.py` → only hits are: (a) line 281 (the lazy import of `build_trust_score_timeline`), (b) line 348 (`blob.get("dominant_delta_pct")` parsing on read), (c) line 352 (`dict(blob["per_axis_delta_pct"])` parsing on read), (d) lines 354-357 (NaN reconstruction on read). Zero hits for `* 100` / `/ 100`. **No inline percentage math anywhere in `signoff_record.py`.**
* Inspected `_parse_drift_attribution` (lines 338-359): three defensive layers — (1) `blob is None` → return None (catches pre-1.1.0 absent field), (2) `not isinstance(blob, dict)` → return None (catches corrupted-to-string fields), (3) `dominant_delta_pct is None` → `float("nan")` (catches the NaN-rendered-as-null sub-floor sentinel from Phase 15 C). **All three legs verified by Probe 3 + the back-compat + NaN-rendering tests.**
* Inspected `write_signoff_record` signature (lines 211-219): parameters are `case_id, reviewer, verdict, notes, *, repo_root, now_utc=None`. **No `drift_attribution_at_signoff_time` kwarg.** This is pinned by `test_write_signoff_record_accepts_no_client_drift_parameter` via `inspect.signature(write_signoff_record).parameters` (not a string match on the source file — robust to formatting changes). **A:-3 LOAD-BEARING confirmed.**
* Inspected `_assert_no_overclaim_in_report` (lines 485-509): walks `json.dumps(_report_to_dict(report)).lower()` which now contains the rendered drift subdict (axis labels + snapshot labels + numeric percentages only). The 4-token envelope forbidden list still applies. **Runtime audit transparently extends to the new field.** Test `test_history_envelope_runtime_audit_clean` pins that a clean envelope (no forbidden-token leak from the new field) survives the audit.
* Inspected the comment block at lines 485-491 of the test file: explicitly documents the C:-8 reasoning — runtime envelope audit is the right pin because the source file legitimately carries the forbidden-token tuples (`_FORBIDDEN_NOTES_TOKENS` + `_ENVELOPE_FORBIDDEN_TOKENS`) as DATA LITERALS. A source-file grep would false-positive on Phase 8 A's audit-subject vocabulary. **Correct interpretation of the C:-1 / C:-8 rubric.**
* Inspected `_schema_versions.py` lines 464-495: the SIGNOFF_RECORD bump-history docstring contains EVERY required clause: (a) "Phase 16 C · 2026-05-17" temporal anchor (line 484), (b) "SERVER-COMPUTED (NOT trusted from a client POST body — A:-3 guard)" posture clause (lines 487-488), (c) "null when fewer than 2 snapshots exist for the case at write time" graceful-degrade clause (lines 489-490), (d) "Closes the audit-trail gap between 'WHAT regressed' (drift surface) and 'WHO judged it' (signoff record)" V:-1 retro-closure clause (lines 490-492), (e) "Pre-1.1.0 consumers that ignore the field continue to function" back-compat clause (lines 492-494). **All E:-1 documentation requirements satisfied.**
* `git status` confirms slice-C is committed (commit bbe3402 on `claude/FM-04a-tier1-ballistic-candidate`). Working-tree-only items are `reports/signoffs/` + `reports/snapshots/` (test artifacts from local runs), `golden_samples/cylinder-pv-candidate/`, `docs/CLAUDE_CODE_HANDOFF_PROMPT.md`, `tests/_test_utils/`, `uv.lock` — all unrelated to slice C. **No accidental mutations from this audit.**

## Adversarial probes

1. **Probe — `inspect.signature` confirms no client drift kwarg.** Ran a standalone `inspect.signature(write_signoff_record)` outside the test file. Output: `Parameters: ['case_id', 'reviewer', 'verdict', 'notes', 'repo_root', 'now_utc']`. `drift_attribution_at_signoff_time` absent. The A:-3 server-computed posture is enforced at the Python level, not just by code review. **PASS.**
2. **Probe — forged-drift kwarg injection.** Called `write_signoff_record(case_id='alpha-candidate', reviewer='attacker', verdict='watching', notes='Tier 1 candidate.', repo_root=repo, drift_attribution_at_signoff_time=<forged DriftAttribution with -999.99 deltas>)`. Result: **`TypeError: write_signoff_record() got an unexpected keyword argument 'drift_attribution_at_signoff_time'`.** A client trying to inject a forged drift gets a Python-level TypeError at the call site — no quiet acceptance, no warning, no silent dropping. **PASS.**
3. **Probe — corrupted on-disk drift field.** Wrote a 1.1.0-stamped signoff JSON with `drift_attribution_at_signoff_time: "NOT-A-DICT-MALFORMED-STRING"` (not None, not a dict, deliberately garbage). Called `read_signoff_history('corrupt-candidate', repo_root=repo)`. Result: **`drift = None`** (graceful degrade). `_parse_drift_attribution`'s `not isinstance(blob, dict)` guard (line 344-345) catches the malformed value and returns None rather than raising. The reader does NOT crash and does NOT propagate corrupt data downstream. **PASS.**
4. **Probe — signoff write does not mutate snapshot tree.** Created a fresh `tmp_path` repo, called `write_signoff_record(case_id='probe-candidate', ...)`. Before the write `reports/snapshots/` did not exist; after the write `reports/snapshots/` still did not exist; `reports/signoffs/probe-candidate/` did exist (the expected output location). The signoff write is correctly siloed to `reports/signoffs/<case_id>/` and never touches `reports/snapshots/` — no risk of write_signoff_record corrupting the very snapshot tree it reads from. **PASS.**
5. **Probe — `inter_snapshot_drift_attribution[-1]` is the load-bearing snap-(N-1) → snap-N pair.** Read `signoff_record.py` lines 286-289 commentary: "Latest consecutive-pair entry is the snap-(N-1) → snap-N transition; this is the load-bearing pair for 'what was regressing at signoff time'." Confirmed against the T:-3 boundary pin in `test_signoff_captures_drift_on_latest_pair_for_leak_case` (lines 266-298): after a 3-snapshot leak arc, the captured drift has `from_snapshot == SNAP_2_LABEL` AND `to_snapshot == SNAP_3_LABEL` (snap-3 chronologically last). **PASS.**
6. **Probe — fresh-arc 2-snapshot exact-value pin.** `test_signoff_drift_dominant_axis_when_only_first_pair_has_drop` asserts on a fresh 2-snapshot timeline (clean snap-1 + canonical snap-2 with energy 15 → 0): `per_axis_delta_pct["energy_audit"] == -100.0`, `dominant_axis == "energy_audit"`, `dominant_delta_pct == -100.0`. **All three exact equalities pass.** Confirms the helper correctly propagates the snap-1 → snap-2 entry when only 2 snapshots exist (only 1 consecutive pair → `inter_snapshot_drift_attribution[-1]` IS that single pair). **PASS.**
7. **Probe — JSON round-trip preservation.** `test_round_trip_read_write_preserves_drift_attribution`: write a signoff on the leak case, read it back via `read_signoff_history`, locate the same record by `signoff_utc`, compare `from_snapshot / to_snapshot / per_axis_delta_pct (dict equality) / dominant_axis`. All four invariants survive write→JSON→read. **PASS.**
8. **Probe — pre-1.1.0 on-disk record reads as None drift.** `test_back_compat_1_0_0_record_reads_as_null_drift`: hand-write a 1.0.0-stamped JSON file with NO `drift_attribution_at_signoff_time` key at all. Call `read_signoff_history`. Result: 1 record returned, `record.drift_attribution_at_signoff_time is None`. The `.get()` lookup at line 331 + `_parse_drift_attribution`'s `blob is None` guard at line 342-343 cooperate to make this graceful. **PASS — back-compat contract honored.**
9. **Probe — NaN dominant_delta_pct renders as JSON null.** `test_record_with_null_dominant_axis_renders_nan_as_null`: write 2 identical snapshots → all axes zero delta → `dominant_axis = None`, `dominant_delta_pct = NaN`. Re-read the JSON file from disk: `payload["drift_attribution_at_signoff_time"]["dominant_delta_pct"] is None` (not NaN, not 0.0, not "NaN" string — proper JSON null). **PASS — E:-3 NaN→null rendering preserved.**

**All probe modifications reverted before completing this report.** No source files were modified during the audit; all 9 probes used either standalone Python invocations, fresh `tempfile.TemporaryDirectory()` repos, or simply read the existing code. Final tree state of the 3 slice-C-scoped files matches commit bbe3402 verbatim.

## Per-axis scoring (63 total)

| Axis | Cap | Score | Rationale |
|---|---|---|---|
| M | 12 | 12/12 | M:-1 schema MINOR bump 1.0.0 → 1.1.0 with full bump-history docstring (`_schema_versions.py` lines 480-494) citing "Phase 16 C · 2026-05-17", the additive `drift_attribution_at_signoff_time` field, the explicit "SERVER-COMPUTED (NOT trusted from a client POST body — A:-3 guard)" posture clause, the null-when-fewer-than-2-snapshots semantics, the "WHAT regressed vs WHO judged it" audit-trail-gap closure, and the pre-1.1.0 back-compat statement. Pinned by `test_signoff_record_schema_at_1_1_0`. M:-2 `_compute_drift_attribution_at_signoff_time` (lines 270-289) is a 20-line thin shim consuming `build_trust_score_timeline(case_id, repo_root).inter_snapshot_drift_attribution[-1]` (Phase 15 C SSOT via the timeline's pre-computed consecutive-pair field) — no inline percentage math anywhere in `signoff_record.py` (grep on `* 100` / `/ 100` returns zero hits in the new code paths). The lazy import (line 281) defers the cost when no snapshots exist. The 2-snapshot threshold guard (line 284) returns None before any field access, preserving graceful degrade. |
| T | 15 | 15/15 | T:-3 boundary pin against the Phase 15 B 3-snapshot leak arc: `test_signoff_captures_drift_on_latest_pair_for_leak_case` asserts `from_snapshot == SNAP_2_LABEL` AND `to_snapshot == SNAP_3_LABEL` AND `per_axis_delta_pct["energy_audit"] == 0.0` AND `per_axis_delta_pct["completeness"] < 0` — the snap-2 → snap-3 transition's load-bearing pair is captured exactly (energy stayed at 0 across the second transition; completeness collapsed from the 3 dropped optional artifacts). Fresh-arc 2-snapshot 15→0 pin: `test_signoff_drift_dominant_axis_when_only_first_pair_has_drop` asserts `per_axis_delta_pct["energy_audit"] == -100.0` AND `dominant_axis == "energy_audit"` AND `dominant_delta_pct == -100.0` exact — pins the "fresh signoff after the FIRST regression event" pattern. Round-trip JSON serialization pin: `test_round_trip_read_write_preserves_drift_attribution` asserts from/to/per_axis_delta_pct/dominant_axis all survive write→JSON→read on the leak case. Graceful-degrade pins both 0-snapshot (`test_signoff_with_no_snapshots_has_null_drift`) and 1-snapshot (`test_signoff_with_one_snapshot_has_null_drift`) cases. 13 tests total — matches blueprint target. All slice-C tests run in 0.52s. |
| C | 12 | 12/12 | C:-1 Tier 1 disclaimer trio preserved on the signoff-history envelope at 1.1.0; pinned by `test_history_envelope_carries_drift_field_at_1_1_0` lines 427-431 (`claim_tier == "Tier 1 engineering candidate"` + `not_signed_validation` in `claim_boundary` + `not_benchmark_agreement` in `claim_boundary`). C:-8 forbidden-token discipline correctly implemented as RUNTIME envelope audit (`test_history_envelope_runtime_audit_clean`), NOT a source-file grep — the comment block at test lines 485-491 explicitly names the reasoning (the module legitimately carries `_FORBIDDEN_NOTES_TOKENS` + `_ENVELOPE_FORBIDDEN_TOKENS` as audit-subject DATA LITERALS, so a source-file grep would false-positive on Phase 8 A's vocabulary). The `_assert_no_overclaim_in_report` runtime guard (lines 485-509 of source) walks the full `_report_to_dict(report)` haystack which now transparently includes the rendered drift subdict; the audit transparently extends to the new field with zero new code paths. C:-3 envelope post-MINOR-bump back-compat: `test_back_compat_1_0_0_record_reads_as_null_drift` writes a 1.0.0-stamped on-disk record with NO `drift_attribution_at_signoff_time` key and confirms it reads cleanly with `drift = None`. The `.get()` lookup at line 331 + `_parse_drift_attribution`'s None-guard at line 342 cooperate. Pre-1.1.0 readers can parse 1.1.0 envelopes without modification (additive field, ignorable). |
| A | 8 | 8/8 | **A:-3 LOAD-BEARING — fully verified.** `write_signoff_record` signature (lines 211-219) parameters are `case_id, reviewer, verdict, notes, *, repo_root, now_utc=None`. **No `drift_attribution_at_signoff_time` kwarg accepted on the caller side.** The helper `_compute_drift_attribution_at_signoff_time(case_id, *, repo_root=repo_root)` is invoked internally with NO callable propagation of any client-supplied data. Pinned by `test_write_signoff_record_accepts_no_client_drift_parameter` via `inspect.signature(write_signoff_record).parameters` (robust to source formatting). Probe 2 confirmed runtime behavior: attempting to inject a forged drift via kwarg raises `TypeError: write_signoff_record() got an unexpected keyword argument 'drift_attribution_at_signoff_time'` — Python-level enforcement, not just code review. The server walks the on-disk snapshot tree via `build_trust_score_timeline(case_id, repo_root)` and reads `inter_snapshot_drift_attribution[-1]` — entirely server-side, zero client surface. A:-2 graceful degrade on 0/1-snapshot cases (returns None, not raises): pinned by both `test_signoff_with_no_snapshots_has_null_drift` and `test_signoff_with_one_snapshot_has_null_drift`. The 2-snapshot threshold guard at line 284 (`if len(timeline.inter_snapshot_drift_attribution) == 0: return None`) handles both cases (0 snapshots → empty timeline → empty inter_snapshot tuple; 1 snapshot → 1 timeline point → empty inter_snapshot tuple). Phase 14 A cross-route signed-registry refusal preserved on the POST path: pinned by `test_write_signoff_refuses_signed_registry` (`write_signoff_record(case_id="GS-001", ...)` raises `ValueError(match="signed.registry")`). |
| E | 8 | 8/8 | E:-1 server-computed posture documented in bump-history (line 487-488: "SERVER-COMPUTED (NOT trusted from a client POST body — A:-3 guard)") AND in the field docstring on the dataclass (lines 196-205: "server-computed; A:-3 NOT trusted from a client POST body") AND in the helper docstring (lines 270-280: "Server-computed only (anti-gaming guard A:-3): the signoff POST body MUST NOT carry a client-side drift_attribution field; the server walks the on-disk snapshot tree at write time"). Three independent documentation surfaces — coherent. E:-2 no real-solver invocation — `_compute_drift_attribution_at_signoff_time` reads ONLY from the snapshot tree (`build_trust_score_timeline` is a pure file-walker); no subprocess spawning, no `solver`/`pyfluent`/`abaqus` import path, no compute. The slice-C test file does invoke `subprocess.check_call` on Phase 15 A generators (which ship Tier 1 candidate decks deterministically) — this is fixture seeding, not solver invocation. E:-3 NaN→null JSON rendering preserved: pinned by `test_record_with_null_dominant_axis_renders_nan_as_null` writing 2 identical snapshots → zero drift across axes → `dominant_axis = None`, `dominant_delta_pct = NaN` → re-read JSON shows `"dominant_delta_pct": null` (proper JSON null, not "NaN" string). `_record_to_dict` (lines 366-371) delegates to the Phase 15 C SSOT `render_drift_attribution_dict` helper — no parallel NaN-handling logic. Back-compat 1.0.0-era on-disk records read as None drift: pinned by `test_back_compat_1_0_0_record_reads_as_null_drift`. |
| V | 8 | 8/8 | V:-1 slice closes Phase 15 retro §3 (drift attribution on more envelopes) for the signoff-record view — explicitly cited in the bump-history docstring (lines 480-494) and the commit message "drift_attribution_at_signoff_time on signoff records". The audit-trail gap between "WHAT regressed" (Phase 15 C drift surface on timeline/alerts) and "WHO judged it" (Phase 8 A signoff record) is now closed by EMBEDDING the snapshot of the WHAT into the immutable WHO record. V:-2 feeds Phase 16 D Journey 1 (drift audit trail e2e) by providing the per-record signoff-side surface the journey test will exercise alongside the slice A/B cohort + timeline cumulative surfaces. V:-3 forbidden-token + Tier 1 trio + HF1 path-guard discipline all preserved: (a) `_assert_no_overclaim_in_report` runtime audit walks the full envelope including new field; (b) Tier 1 trio preserved on the 1.1.0 envelope (pinned by line 427-431 of the test); (c) HF1.7a / HF1.8 path guards: `_assert_not_in_golden_samples` (lines 412-419) untouched, signoff writes refused under `golden_samples/**`; signed-registry refusal preserved (Phase 14 A SSOT not regressed). The 2496-test full sweep is green at the blueprint-expected count exactly (slice-B baseline 2483 + 13 new = 2496). |
| **Total** | **63** | **63/63** | All axes at cap; every binding floor exceeded with margin. |

## Verdict

**APPROVE.**

Slice C scores **63/63**, every axis at full credit (M 12/12, T 15/15, C 12/12, A 8/8, E 8/8, V 8/8). All binding floors (M ≥10, T ≥10, C ≥10, A ≥6, E ≥7, V ≥7) cleared with margin. The stop condition ≥60/63 AND every axis ≥ binding-floor is met decisively. The full 2496-test sweep stays green at the blueprint-expected count exactly (slice-B baseline 2483 + 13 new = 2496). The A:-3 LOAD-BEARING server-computed posture is verified at four independent layers: (1) `write_signoff_record` signature has no drift kwarg (Probe 1 inspect.signature), (2) attempting to inject a forged drift via kwarg raises TypeError at the Python level (Probe 2 runtime), (3) the server-side helper reads from `build_trust_score_timeline(...).inter_snapshot_drift_attribution[-1]` — a pure file-walker over the on-disk snapshot tree, no client surface (source code inspection), (4) the field-level docstring + helper docstring + bump-history docstring all independently state the SERVER-COMPUTED posture (E:-1 three-surface coherence). Nine adversarial probes all behaved correctly: A:-3 inspect.signature pin, forged-kwarg TypeError, corrupted-JSON graceful degrade, snapshot-tree not mutated, snap-(N-1)→snap-N load-bearing pair confirmed, fresh-arc exact-value pin, JSON round-trip preservation, pre-1.1.0 back-compat None, NaN→null rendering. All probes used standalone Python invocations or fresh `tempfile.TemporaryDirectory()` repos — **no source files were modified during the audit**; final tree state of the 3 slice-C-scoped files matches commit bbe3402 verbatim.

## Open observations (non-blocking)

* The `drift_attribution_at_signoff_time` field on the `SignoffRecord` dataclass (line 195) is typed `object = None` rather than `DriftAttribution | None`. This mirrors the Phase 16 A `cumulative_drift_attribution: object = None` typing in `TrustScoreTimeline` and the Phase 16 B `cohort_drift_attribution: object = None` typing in `CohortAnomaliesReport` — all three Phase 16 surfaces use the same `object` typing to defer the `DriftAttribution` import (which itself imports from `trust_score_drift_attribution`, creating a potential cycle with consumers that the trust_score_timeline module already participates in). Runtime behavior is correct; a future janitorial pass could tighten typing once the cycle is broken. Coherent with the slice-A and slice-B observations in A.md / B.md.
* The `_parse_drift_attribution` helper (lines 338-359) defensively handles three failure modes: (a) None blob (pre-1.1.0 record), (b) non-dict blob (corrupted-to-string field — Probe 3), (c) `dominant_delta_pct is None` (NaN-rendered-as-null sub-floor sentinel from Phase 15 C). Legs (a) and (c) are explicitly pinned by tests (back-compat + NaN-rendering); leg (b) is only pinned by my Probe 3 adversarial test, not by a shipped test. A future-pass janitorial improvement would be to add a `test_parse_drift_attribution_returns_none_on_malformed_blob` shipped pin — not blocking for slice C (the runtime behavior is correct and the leg is small).
* The slice-C tests reuse the Phase 15 A generator scripts (`gen_rod_wave_impact_deck.py` + `gen_rod_wave_impact_energy_leak_deck.py`) via `subprocess.check_call` to seed deterministic decks. This is the canonical pattern in the Phase 15 / Phase 16 test suite — fixture seeding (not solver invocation), and it produces version-controlled Tier 1 candidate decks. The `leak_arc_repo` module-scoped fixture (lines 220-263) is the heaviest in the slice (3 snapshot writes via `write_cohort_snapshot` across the 3 ISO labels) but module-scope amortization keeps total runtime at 0.52s for all 13 tests.
* The `_compute_drift_attribution_at_signoff_time` helper (lines 270-289) reads `timeline.inter_snapshot_drift_attribution` — a Phase 15 C tuple-of-`DriftAttribution`. The slice-C consumer relies on the Phase 15 C SSOT's behavior of producing a tuple in chronological order (snap-1→snap-2, snap-2→snap-3, ...), so `[-1]` is always the snap-(N-1)→snap-N latest pair. This dependency on the SSOT's ordering convention is implicit (not pinned by an integration test in `signoff_record.py`'s test file); it IS pinned by the Phase 15 C test file but a future-pass observation: a "chronological-order regression in `build_trust_score_timeline` would silently corrupt the slice-C signoff drift capture" risk note would be worth adding to the Phase 16 E retrospective. Not blocking — the Phase 15 C tests cover the SSOT's chronological ordering, so a regression there would be caught by the full sweep.
* The runtime envelope audit (`_assert_no_overclaim_in_report` at lines 485-509 of source) walks `json.dumps(_report_to_dict(report)).lower()` which now includes the rendered drift subdict. The drift subdict contains only axis labels (`"completeness"` / `"convergence_stability"` / `"energy_audit"` / `"convergence_completeness_signature"`), snapshot labels (ISO timestamps like `"2026-05-17T120000Z"`), and numeric percentages — none of which are forbidden tokens. The new field cannot introduce forbidden-token leak via its native shape. Pinned by `test_history_envelope_runtime_audit_clean`. Coherent with the slice-A and slice-B observations on the runtime-audit transparent-extension property.
* The slice-C test file's docstring (lines 1-32) cross-references the binding rubric's M/T/C/A anti-gaming guard codes (M:-1, A:-3, T:-3, C:-1, back-compat) so future auditors can map test → rubric clause without spelunking. Continues the Phase 11+ practice — matches the slice-B test file's structure.
* The 1.0.0 → 1.1.0 bump for SIGNOFF_RECORD aligns with the 2026-05-17 sweep that also bumped TRUST_SCORE_TIMELINE 1.0.0 → 1.1.0 (Phase 15 C), then 1.1.0 → 1.2.0 (Phase 16 A), and TRUST_SCORE_ALERTS 1.0.0 → 1.1.0 (Phase 15 C), and COHORT_ANOMALIES 1.0.0 → 1.1.0 (Phase 16 B). All five Phase 15-16 MINOR bumps are additive `drift_attribution` field additions in different envelopes — same pattern, same back-compat contract, same SSOT-via-Phase-15-C-`compute_drift_attribution`. Coherent program-wide.
