# Phase 12 F — TAA report

**Slice commit:** `ee2acbb`
**Verdict:** APPROVE
**Test count:** 43 targeted (3 journey + 40 in `test_phase11_endpoints_integration.py`); the slice itself adds 16 new tests (3 journey + **13** supplemental — note: commit body §"Twelve new Phase 12 F supplemental tests" undercounts by one, the actual `grep -nE "^def test_p12f_"` returns 13 distinct test functions, which still satisfies the blueprint floor of ≥12).
**Test pass:** 43/43 targeted; full backend sweep `2187 passed, 7 skipped, 3 warnings in 24.38s` — matches commit-body claim "+16 vs slice-E baseline 2171 = 2187" exactly, zero regressions
**Schema bumps:** none — slice F is a pure test-layer slice; no `schema_version` constants change, no domain code or routes touched (full backend sweep referenced in commit body as `2187 passed, 7 skipped` reconciles with the slice-E `2171 passed, 7 skipped` baseline + 16 net new tests)

## Per-axis evidence

- **M (11/12)** — SSOT discipline is strong. The slice pins the `TREND_AXES` import + a module-level `_TREND_COMPLETENESS_AXIS` constant with an `assert` statement at import time (`test_fm04a_phase12_cohort_journeys_e2e.py:100-108`) — a future PR that renames or removes the `completeness` member of `TREND_AXES` would fail to load the test module entirely, surfacing as a loud collection-time error rather than a silent stale-literal slip. The journey set reuses slice-D's `write_cohort_snapshot` + `SnapshotCaseInput` API + `FILLER_COHORT_CASES` rationale + degraded-PV-extended helper pattern (`_degraded_pv_extended_input`, lines 247-284) without duplicating the snapshot-writer logic — the test module imports the snapshot service rather than reimplementing it. The route-count contract is auditable per-journey via the explicit `routes_crossed: set[str]` (J4 and J6) / `set[tuple[str, str]]` (J5) accumulator pattern with a binding `assert len(routes_crossed) >= N` at the end — the count is not implied by the call sequence alone, it's a tracked-set cardinality. The trend-axis SSOT pin is correctly named (the short form `"completeness"` matches the `TREND_AXES` enum, not the long-form `completeness_weighted` field on the underlying `TimelinePoint` dataclass — the test docstring explicitly disambiguates this at lines 575-580). **One M-axis defect (-1)**: Journey 4's `routes_crossed` set uses synthetic distinct keys `"cohort-executive-summary"` + `"cohort-executive-summary-rread"` to count the initial poll and the re-poll as TWO distinct routes; the actual URL is the same (`GET /api/v1/cohort-executive-summary`). The slice ships 5 distinct `routes_crossed.add(...)` calls but the underlying distinct URL count is 4 (cohort-executive-summary, cohort-anomalies, advisor-critique, signoff-history-POST). The blueprint §3.F deliverable says "≥5 routes" without disambiguating "distinct URLs" vs "distinct route calls" — the ambiguity is resolved in the slice author's favor (5 calls) but a stricter reading would count 4 distinct URLs. The honest engineering observation is that the re-poll IS load-bearing (it pins the verdict-landed-on-row state mutation), but the route-count audit framing as "5 distinct routes" overstates the URL diversity. This is M-axis (SSOT discipline) rather than C-axis (coverage), because the route URLs themselves are documented per-step in the docstring — but the auditable counter is not strictly counting URL diversity, it's counting temporally-distinct calls.

- **T (13/15)** — Test coverage is broad and the test docstrings explicitly call out the X:-2 load-bearing rule per journey. T:-3 evidence: each journey crosses at least the blueprint-stipulated route count with binding `assert len(routes_crossed) >= N` cardinality pins (J4=5, J5=6, J6=4); the 3 journeys collectively exercise 5 distinct route surfaces (`cohort-executive-summary` / `cohort-anomalies` / `advisor-critique` / `signoff-history` / `cohort-trend-anomalies` / `case-completeness` — 6 actually) end-to-end through the real ASGI stack via `httpx.ASGITransport`. The 13 supplemental tests (commit body says "twelve") cover: 3× advisor-critique 422 exact-code (invalid case_id shape, special chars, empty snapshot value), 3× advisor-critique 405 exact-method (PUT, DELETE, PATCH), 2× case-completeness 400 exact-code (invalid case_id shape, special chars), 2× case-completeness 405 exact-method (PUT, DELETE), 1× advisor-critique 422 signed-registry detail content audit (`signed-registry` + `candidate` + `out of scope` triple), 1× case-completeness signed-registry honest-gap audit (asserts `< 500`, flagged for slice-G alignment), 1× advisor-critique snapshot-label case-sensitivity (lowercase-T-Z refusal) — 13 distinct gates, of which 12 are exact-code tightenings of the Phase 11 D permissive ranges and 1 (the case-completeness signed-registry test) is an honest gap audit. The cross-case independence audit in Journey 5 (lines 540-559) pins that modal vs linear_static_pv advisor outputs are TEXTUALLY DISTINCT — catching a regression where the advisor stub gets boilerplate. The independence audit re-issues the 3 advisor-critique calls (instead of caching the responses from the earlier loop at lines 477-520) which is a minor inefficiency but not a coverage defect. **One T-axis defect (-1)**: the case-completeness signed-registry test (`test_p12f_case_completeness_route_4xx_signed_registry_refusal`, lines 677-703) asserts only `res.status_code < 500` rather than an exact 4xx code — the test docstring frames this as an "honest gap audit" but the section header at line 529 says "Phase 12 F supplemental integration tests — tightening 4xx ranges". The framing-vs-assertion mismatch is real (one of the 13 supplementals does NOT tighten any range; it documents that the gate is missing). This is honest engineering posture, not gaming — the test does pin the contract at "not 5xx" + flags the slice-G alignment target in the docstring — but the slice ships 12 actual tightenings + 1 honest-gap test, not 13 tightenings. **One additional T-axis defect (-1)**: the cross-case independence audit in Journey 5 (lines 540-559) re-fetches the 3 advisor-critique responses that were already cached in the earlier loop; a 6-line refactor to capture the failure-mode text in the first loop and assert on the concatenations afterwards would be the cleaner pattern, but more importantly, the current independence audit assertion is `modal_failures_concat != pv_failures_concat` which only catches the trivial case of TOTAL equality. A future regression that surfaces 80% boilerplate + 20% analysis-type-specific text would pass this assertion (the strings would differ but the load-bearing analysis-type-specificity would be diluted). A tighter assertion would be e.g. `assert "plasticity" not in modal_failures_concat` AND `assert "mass participation" not in pv_failures_concat` — pinning that modal-keyword leakage into PV results (and vice versa) is forbidden. The current `!=` assertion is at the floor of meaningful, not load-bearing.

- **C (12/12)** — Forbidden-token grep clean. `grep -nE "validated against|perforation completed|bullet-through-steel complete|validated physics|production ready|certified|approved for service|ASME compliant|signed off" tests/test_fm04a_phase12_cohort_journeys_e2e.py` returns **zero** hits; the same grep over the Phase 12 F supplemental section (`sed -n '529,720p' tests/test_phase11_endpoints_integration.py | grep ...`) also returns zero. The Tier 1 disclaimer trio audit is present on EVERY 200 envelope inspected by the 3 journeys (verified by `grep -nE "claim_tier|claim_boundary|claim_impact"`): Journey 4 audits `summary_body` (line 379-380), `anomalies_body` (line 390-391), `adv_body` (line 403-404), `summary_body2` (implicit through `cases` iteration — minor C-axis observation, no direct re-check of the trio on the re-poll); Journey 5 audits `adv_body` (lines 486-488 — full trio, all three keys) and `comp_body` (line 530, `claim_tier` only — partial trio); Journey 6 audits `trend_pre_body` (line 614), `history_body` (line 653), `trend_post_body` (no direct trio check at line 685 — but a 200 status is asserted at line 684). The disclaimer-trio audit is partial in 3 sites (re-polls of cohort-executive-summary, comp_body partial check, trend_post_body), but those routes are the same ones the prior journeys already audited within the same test session — the disclaimer trio is preserved on those routes at first call. The case_id fixture set (lines 167-177) uses ONLY `*-candidate` forms (`cylinder-pv-candidate`, `modal-cantilever-candidate`, `modal-cantilever-stiff-candidate`, `cylinder-pv-extended-candidate`, `synthetic-cohort-bulk-01-candidate`, `synthetic-cohort-bulk-02-candidate`) — zero `^GS-\d{3}$` signed-registry case_ids in fixtures. The `GS-042` and `GS-101` URL string args in the supplemental section (lines 667, 693) are used ONLY for NEGATIVE testing of the refusal gate (asserting they return 422 or 4xx); no fixtures are created at those paths. The Phase 11 retro §4 carry-forward (tighten the Phase 11 D permissive `400 <= code < 500` ranges) is substantively closed by 12 of the 13 supplemental tests (the case-completeness signed-registry test is the named honest-gap exception). **C:-8 substantively satisfied.**

- **A (7/8)** — Five of the six named anti-gaming guards from blueprint sub-rubric §4.F are exercised distinctly. **M:-2** (route-count auditable not implied): the three `routes_crossed.add(...)` accumulators + cardinality assertions per journey (lines 437-440, 535-538, 705-708) pin the contract at the tracked-set level; a future refactor that drops a route call surfaces immediately as a cardinality miss. **T:-3** (route counts ≥ blueprint floor): the three pins are `>= 5`, `>= 6`, `>= 4` — exact match for blueprint §3.F. **C:-8** (Tier 1 disclaimer trio on every 200 envelope + signed-registry refusal honestly tested): 10 of 11 `claim_tier` / `claim_boundary` / `claim_impact` audit sites are load-bearing (the comp_body audit at line 530 is partial — only `claim_tier`, not the full trio). **A:-3** (signed-registry refusal): the supplemental test at line 658-674 pins `signed-registry` + `candidate` + `out of scope` as a 3-term content audit on the 422 detail — this is the load-bearing A-axis test of the slice. **E:-2** (no real-solver, LLM-offline pass): every advisor critique 200 envelope lands at `advisor_status=='stub'` implicitly (the test suite runs with no advisor backend configured; the journey docstrings name this explicitly at lines 56-58 of the module docstring). **V:-3** (honest engineering posture for Journey 6): the recovery-doesn't-zero-alarm caveat is explicitly named in the docstring at lines 596-602 and pinned in the assertion at lines 676-679 + 693-696 + the explicit `else` branch handling the alarm-flushed-entirely case (lines 697-701) — this is a real honest-engineering posture call (not gaming), where the math doesn't always produce a clean "alarm cleared" with 4 points `[baseline, mild, big, restored]`. **One A-axis defect (-1)**: Journey 6's slope-comparison assertion at lines 676-679 (and 693-696) is wrapped in `if target_events_4pt:` / `if post_events_for_target:` conditional guards — if the recovery DOES flush the alarm entirely (the `else` branch at line 697), the test passes with NO slope-comparison assertion fired. The honest engineering posture is correct (the math doesn't always deliver "less negative slope"), but the resulting assertion is conditional rather than universal: a regression where the recovery snapshot fails to be written and the trend events stay identical to slice-D's pre-recovery state would also fall into the empty-event-list branch and pass silently. A safer pattern would be `assert (no_events_at_all) OR (slope_after > slope_before)` so the recovery has SOME observable effect — the current pattern allows "no observable change" to pass.

- **E (8/8)** — Full backend sweep `2187 passed, 7 skipped, 3 warnings in 24.38s`, zero regressions vs slice-E baseline of 2171 passed (+16 net additions, matching the 3 journey + 13 supplemental = 16 collected exactly). `git show ee2acbb --stat` confirms **only** `.planning/phase12_audit_reports/E.md` (archive of prior slice TAA report) + `tests/test_fm04a_phase12_cohort_journeys_e2e.py` (new) + `tests/test_phase11_endpoints_integration.py` (supplemental section added) are touched — **no** `golden_samples/GS-\d{3}/` paths, **no** `scripts/hf1_path_guard.py` modification, **no** `HF1_GUARD_OVERRIDE` env var in the commit body. No real backend / solver / LLM invocation in the new tests: the test file imports only `app.api.routes` modules + `app.services.reporting.cohort_snapshot` + `app.services.reporting.cohort_trend_anomalies` + the `app.main:app` ASGI factory; no `subprocess`, `Popen`, `os.system`, no `anthropic` / `openai` SDK imports, no live API endpoints (verified by `grep -nE "real.solver|subprocess|abaqus|nastran|calculix|ccx |Popen|os\.system" tests/test_fm04a_phase12_cohort_journeys_e2e.py` returning empty + `grep -nE "anthropic|openai|requests\.post.*api" tests/test_fm04a_phase12_cohort_journeys_e2e.py` returning empty). The `tmp_path` discipline is robust: the `fake_repo` fixture (lines 146-158) monkeypatches `_repo_root` on all 6 routes — no risk of accidental writes to the real `reports/snapshots/` or `golden_samples/` tree. The `_seed_repo` helper (lines 183-210) writes a self-contained `tmp_path/golden_samples/` tree as the cohort root; the filler-rename pattern (lines 199-209) avoids double-counting canonical case_ids. The synthetic fillers (`synthetic-cohort-bulk-01-candidate`, `synthetic-cohort-bulk-02-candidate`) reuse the slice-D pattern of lifting the cohort above the n=4 statistical floor for both z-score and trend-points analyses. ASGI client harness (`_SyncASGIClient`, lines 115-138) uses `asyncio.run` per call which is a known anti-pattern for high-throughput suites but acceptable at this slice's scale (3 journeys × ≤10 route calls each = ~30 ASGI invocations, completing in <1.2s). No demo orchestrator / no scope creep beyond §3.F — slice F ships exactly what its blueprint names.

- **V (7/8)** — TAA verdict APPROVE, with two coherence concerns documented and the honest engineering posture in Journey 6 rewarded. The commit body §"Honest engineering posture (V:-3): recovery is asserted as 'less negative slope' not 'alarm cleared' — the math doesn't always zero out with 4 points" is verified by reading the actual journey code (lines 676-701): the `if/else` structure makes the relaxed contract explicit, not hidden. This is a real engineering call (LSQ-fitted slopes over 4 points with 2 outlier-heavy intermediate snapshots don't always cross the `TREND_SLOPE_INFO_MAX = -0.5` threshold), and the slice author's choice to document it in the docstring + ship a conditional assertion is the HONEST move — a gaming author would have either (a) silently relaxed the assertion to "no anomaly at all" and let the conditional fall through, or (b) tuned the synthetic snapshots until the alarm DID clear and pretended the math always works. Slice F does neither. **Two V-axis concerns documented**: (a) The supplemental test count is 13, not the 12 claimed in the commit body §"Twelve new Phase 12 F supplemental tests" — the slice OVER-delivers by 1 test, but the count mismatch shows the commit body wasn't re-checked against `grep -nE "^def test_p12f_"` (13 hits). The +16 net test count IS correctly reconciled later in the commit body via "+1 re-discovered after rerun", but the section header line under-counts. This is minor over-delivery, not under-delivery, and the slice author flagged the +1 anomaly honestly. (b) Journey 4's route count of 5 is achieved via the synthetic-key trick (`cohort-executive-summary` + `cohort-executive-summary-rread` as two distinct entries in `routes_crossed`); the underlying distinct URL count is 4. A stricter reading of the blueprint's "≥5 routes" deliverable would deduct here. The resolution is that the re-poll IS load-bearing (it pins the verdict-landed-on-row state mutation, which is the journey's binding "after" measurement), so counting it as a distinct route call is defensible — but the framing as "5 distinct routes" is slightly aspirational. Neither concern is load-bearing for APPROVE: the test count is +1 over delivery, and the Journey 4 route count is at the floor with a defensible interpretation. 7/8 (at floor — the honest engineering posture in Journey 6 + the route-count framing in Journey 4 are both at-floor V-axis items).

## Findings

- **MEDIUM** — Journey 6's slope-comparison assertion is wrapped in conditional `if target_events_4pt:` / `if post_events_for_target:` guards (`test_fm04a_phase12_cohort_journeys_e2e.py:674-701`). The honest engineering posture is correct — recovery doesn't always produce a less-negative slope when LSQ-fitting 4 points `[baseline, mild, big, restored]` — but the resulting assertion pattern allows TWO degenerate cases to pass: (a) the recovery snapshot fails to be written and the trend events stay identical to the pre-recovery state (would fall into the empty-`target_events_4pt` branch), (b) the recovery snapshot lands but the slope STAYS as negative as before (no recovery effect, but the test still passes via the `else: pass` branch). **Impact**: a regression where `_write_recovery_snapshot4` silently fails (e.g. the `expected_results.json` paths break, the writer raises but is caught somewhere) would not be caught by Journey 6 — the trend events would simply stay unchanged and the conditional assertion would skip. **Suggested fix for slice G**: add a pre-condition assertion that `_write_recovery_snapshot4` actually emitted a 4th snapshot under `tmp_path/reports/snapshots/<label>/` (a 2-line `assert (fake_repo / "reports" / "snapshots" / snapshot4_label).exists()` would pin the state mutation independently of the slope check). The current pattern is honest engineering on the LSQ slope math, but the conditional structure adds a separate gap (recovery happened but was undetectable).

- **LOW** — The cross-case independence audit in Journey 5 (lines 540-559) asserts only `modal_failures_concat != pv_failures_concat` — a regression where the advisor stub produces 80% shared boilerplate + 20% analysis-type-specific text would pass this assertion (the concatenated strings would differ but the load-bearing analysis-type-specificity would be diluted). Suggested fix: add cross-leakage forbidden-keyword pins (e.g. `assert "plasticity" not in modal_failures_concat` AND `assert "mass participation" not in pv_failures_concat`) so the independence audit catches partial dilution, not just total equality. Slice G retro carry-forward.

- **LOW** — The case-completeness signed-registry test (`test_p12f_case_completeness_route_4xx_signed_registry_refusal`, lines 677-703) asserts only `res.status_code < 500` — this is documented as an "honest gap audit" of a missing route guard, but the section header at line 529 of `test_phase11_endpoints_integration.py` frames the 13 supplementals as "tightening 4xx ranges". One of the 13 is NOT a tightening — it's a gap-documentation test. The framing-vs-assertion mismatch is honest engineering posture (the slice author flags the slice-G alignment target in the test docstring), but a future reader counting "13 exact-code tightenings" would be off by one. Suggested fix for slice G: either (a) align case-completeness with advisor-critique by adding a 422 signed-registry guard + flip this test to assert exact 422, or (b) rename the test to `test_p12f_case_completeness_signed_registry_gap_audit` so the framing matches the assertion strength. Slice G retro carry-forward.

- **LOW** — Journey 4's route count of 5 is achieved by using two synthetic-distinct keys (`cohort-executive-summary` + `cohort-executive-summary-rread`) for the same underlying URL `GET /api/v1/cohort-executive-summary`. The distinct-URL count is 4. The blueprint §3.F deliverable says "≥5 routes" — the resolution depends on whether "routes" means "distinct URL paths" or "distinct route calls". The slice author's interpretation (distinct route calls) is defensible because the re-poll is load-bearing (it pins the verdict-landed-on-row state mutation), but a stricter blueprint reading would prefer 5 distinct URLs (e.g. by replacing the re-poll with a `case-completeness` call). Slice G retrospective should resolve the blueprint ambiguity or accept the slice-F interpretation as the precedent. Not load-bearing for slice-F APPROVE since the journey IS substantively 5 calls + the re-poll IS load-bearing — but a reviewer reading the commit body's "Journey 4 (cohort triage): ≥5 routes" without reading the actual test code might believe 5 distinct URLs.

- **LOW** — Commit body §"Twelve new Phase 12 F supplemental tests" under-counts the actual delivery (`grep -nE "^def test_p12f_"` returns 13). The +16 net test count is correctly reconciled later via "+1 re-discovered after rerun", but the section-header count and the test list itemized below it should be 13. Minor over-delivery; not a defect, just an out-of-date commit body section. Slice G retrospective should not carry this forward as a real risk.

## Cumulative slice F axes

M + T + C + A + E + V = 11 + 13 + 12 + 7 + 8 + 7 = **58 / 63**

Per-axis floor check (slice-F sub-rubric):
* M 11 ≥ 10 ✓
* T 13 ≥ 12 ✓ (above floor — 16 actual new tests; 3 journeys + 13 supplemental)
* X (load-bearing X:-2 anti-gaming guard verified at 3 independent assertion sites — Journey 4 routes_crossed cardinality ≥5; Journey 5 (route, case_id) tuple set cardinality ≥6; Journey 6 routes_crossed cardinality ≥4 — three distinct route-count contracts pinned via tracked-set cardinality assertions, not implied by call sequence)
* C 12 ≥ 10 ✓
* A 7 ≥ 6 ✓ (at floor — the Journey 6 conditional-slope-assertion structure is the binding constraint; the honest engineering posture is correct, but the conditional structure allows a recovery-not-happening regression to pass silently)
* E 8 ≥ 7 ✓
* V 7 ≥ 7 ✓ (at floor — the honest engineering posture in Journey 6 + the route-count framing in Journey 4 are both at-floor V-axis items; rewarded for honesty, deducted for the conditional-assertion + synthetic-key structure)

Verdict: **APPROVE**

All axes meet floor. Zero HIGH findings. One MEDIUM finding (Journey 6's conditional slope-assertion structure could mask a recovery-not-happening regression — slice G should add a pre-condition assertion that the 4th snapshot was actually written). Four LOW findings, all suitable for slice-G retrospective carry-forward.

## Engineering coherence

Slice F substantively closes its blueprint contract: three load-bearing reviewer journeys that compose the slice-A/B modal axis + slice-C real-runnable candidate cases + slice-D multi-snapshot anomaly triggers + slice-E cohort dashboard client surface end-to-end through the real ASGI stack, with explicit auditable route counts (5/6/4) and Tier 1 disclaimer trio audits on 10 of 11 inspected 200 envelopes. The 12-of-13 supplemental tightenings close the Phase 11 retro §4 carry-forward (permissive `400 <= code < 500` → exact-code 400/422/405 contracts). The honest engineering posture in Journey 6 — explicitly documenting that recovery doesn't always zero out the alarm with 4 LSQ-fitted points and shipping a "less negative slope" assertion instead of an "alarm cleared" assertion — is the exemplary anti-gaming move in this slice: the slice author chose to ship the contract the math actually delivers, not the aspirational blueprint wording. The MEDIUM finding (conditional slope-assertion masking a recovery-not-happening regression) is the binding slice-G carry-forward; the LOW findings are minor structural/framing items that don't undermine the slice's value. Tests pass cleanly (43/43 targeted + 2187/2187 full backend), no real-solver invocation, no signed-registry case_ids in fixtures, Tier 1 disclaimer trio preserved — the four-question gate is honored throughout.

## Residual risks for next slices

1. **Journey 6's conditional-slope-assertion structure** (MEDIUM): the `if target_events_4pt:` / `if post_events_for_target:` guards allow a recovery-not-happening regression to pass silently. Slice G must add a pre-condition assertion that the 4th snapshot was actually written under `tmp_path/reports/snapshots/<label>/`.
2. **Cross-case independence audit dilution** (LOW): the `!=` assertion only catches total equality. Slice G should add cross-leakage forbidden-keyword pins so partial dilution is also caught.
3. **Case-completeness signed-registry guard missing** (LOW): the route currently has no explicit signed-registry refusal. Slice G should align it with advisor-critique by adding a 422 guard + flipping the gap-audit test to an exact-code assertion.
4. **Journey 4 route-count framing ambiguity** (LOW): the 5-routes-claim is achieved via the re-poll synthetic-key trick. Slice G retrospective should resolve the blueprint ambiguity (distinct URLs vs distinct route calls) or accept the slice-F interpretation as the precedent.
5. **Slice E MEDIUM carried forward**: the `CohortDashboardPanel.tsx` visual restructure (blueprint #07 fidelity) is still deferred. Slice F is a pure backend/test slice — it did not touch the frontend. Slice G retains this as its load-bearing visual deliverable.
6. **Slice D MEDIUM carried forward**: the `_convergence_weighted` modal+linear_static direct unit-test gap remains open. Slice F did not touch backend domain code.
7. **All earlier slice LOWs carried forward**: theme tokens fragmentation, composite-drift test missing, severity-bucket SSOT, parallel-pipeline-audit checklist, snapshot-1-bucket-distribution direct pin — all open after slice F. Slice G retrospective should batch-close.
