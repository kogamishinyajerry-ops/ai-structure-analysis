# Phase 12 D — TAA report

**Slice commit:** `2632216`
**Verdict:** APPROVE
**Test count:** 14 collected (12 distinct test functions; 1× `@parametrize` over the 3-tuple `[0, 1, 2]` of snapshot indices on the Tier 1 disclaimer audit expands 1→3, giving 12 − 1 + 3 = 14 — matches `pytest -v` exactly)
**Test pass:** 14/14; full sweep `2171 passed, 7 skipped, 3 warnings in 22.59s` (slice-C baseline was 2157 passed → +14 net, no regressions; matches commit-body verification claim exactly)
**Schema bumps:** none — slice D ships behavior changes (per-case `analysis_type` dispatch + modal+linear_static convergence in the timeline scorer) that mirror existing trust_score.py Phase 11 A / Phase 12 A logic. No new schema_version is created and `COHORT_SNAPSHOT_MANIFEST_SCHEMA_VERSION` is asserted-as-SSOT by the new pin test (closing Phase 11 retro §3).

## Per-axis evidence

- **M (11/12)** — SSOT discipline is strong. `PHASE12_FIXTURE_CASES: tuple[tuple[str, str], ...]` (lines 55-60) is a closed enum of `(case_id, analysis_type)` 4-tuples and is the single source for every snapshot construction loop (snap_idx 0/1/2 dispatchers all iterate over it). `FILLER_COHORT_CASES: tuple[str, ...]` (lines 68-77) is the single source for the two synthetic background cases and carries a 9-line docstring explaining the n=4 statistical ceiling motivation (closes the "why are these cases here?" question reviewers would otherwise ask). `_full_case_input`, `_degraded_pv_extended_input`, `_outlier_modal_stiff_input`, `_seed_repo`, `_write_three_snapshots` centralize all the cohort-construction math through one set of helpers. The new `SnapshotCaseInput.analysis_type` field (`cohort_snapshot.py:93`) is a named dataclass field with a back-compat default of `"ballistic"` and a 7-line docstring naming Phase 12 D as the introducing slice + the artificial-trust-lowering motivation. The pass-through at `cohort_snapshot.py:177-184` includes a 4-line inline comment naming the modal-rubric dispatch rationale. The new modal+linear_static branches in `_convergence_weighted` (`trust_score_timeline.py:198-232`) carry a 5-line inline comment naming the trust_score._score_convergence_axis source as the mirror authority. The `COHORT_SNAPSHOT_MANIFEST_SCHEMA_VERSION` SSOT pin test (`test_snapshot_manifest_schema_version_reads_from_ssot`, lines 275-286) cites Phase 11 retro §3 explicitly. **One M-axis defect (-1)**: the test file hard-codes the 2-axis severity-bucket strings `("info", "warn", "danger")` (lines 424, 477, 505) as inline tuples rather than importing them from `cohort_anomalies` / `cohort_trend_anomalies`. A future addition of a fourth severity bucket (e.g. "critical") would not surface here, and the SSOT bucket-name source isn't named in any constant import in the slice-D test. Mitigated by the bucket-list being a closed set in the existing builders (`grep severity.*= cohort_anomalies.py` shows the dispatch happens via threshold comparison, not enum) — but slice C's pattern of importing every threshold from the source module set a higher bar that slice D didn't fully match.

- **T (13/15)** — Per-arc coverage is comprehensive and the test docstrings are unusually honest about the statistical-impossibility / aspirational-blueprint-wording gap. T:-3 evidence-status pins: each alarm payload's `case_id` + `axis` + `severity` is asserted distinctly (`test_z_score_outlier_alarm_payload_pins_case_axis_severity`, lines 480-505). T:-4 per-snapshot envelope pins (the Tier 1 disclaimer trio parametrize at lines 513-526 fires `claim_tier == "Tier 1 engineering candidate"` + `"not_signed_validation"` substring + `"not_benchmark_agreement"` substring on all three snapshot manifests). The dashboard #07 "both alarm surfaces firing simultaneously" scenario is pinned by `test_both_alarm_kinds_fire_in_same_arc` (lines 534-558) — both the trend-slope alarm (on cylinder-pv-extended) and the z-score outlier (on modal-stiff energy_audit) must appear in the same arc. The degradation trajectory pin (`test_snapshot3_cylinder_pv_extended_trust_score_visibly_degraded`, lines 347-386) asserts (a) point_count==3, (b) `latest_trust < baseline_trust`, and (c) `latest_pt.completeness_weighted < base_pt.completeness_weighted` — three distinct progressive-degradation observables. The healthy-baseline pin (`test_snapshot3_pv_baseline_still_healthy`, lines 389-397) confirms `cylinder-pv-candidate.bucket == "healthy"` after the arc — i.e. the degradation injection is correctly scoped to cylinder-pv-extended and modal-stiff only. The schema_version SSOT pin (lines 275-286) closes Phase 11 retro §3 — `manifest["schema_version"] == COHORT_SNAPSHOT_MANIFEST_SCHEMA_VERSION` is a literal-vs-import test that catches a hardcoded-literal regression. **Two T-axis defects (-2)**: (a) **No test asserts the `_convergence_weighted` modal+linear_static branches return the documented `100`/`30`/`0` raw scores directly.** The Phase 12 A audit (RTAA C.md slice-A T-axis evidence) flagged the same gap on `_score_convergence_axis`'s new branches — slice D extends the gap from one scorer to the timeline scorer. The branches are exercised indirectly through `test_snapshot3_cylinder_pv_extended_trust_score_visibly_degraded` (linear_static path) and the modal-cantilever fixtures' timeline points (modal path), but no test pins `_convergence_weighted({"convergence_kind": "modal", "mode_count_sweep": {...}})` returning the expected weighted score. A unit-style direct test on `_convergence_weighted` would close this gap in three lines per branch. (b) **No test asserts the cohort_executive_summary regressed-bucket count is `0` on snapshot 1.** The blueprint §3.D step language describes snapshot 1 as "every case healthy" but the only snapshot-1 assertion (`test_snapshot1_all_cases_healthy`, lines 327-344) checks (i) `summary.cohort_count == 6` and (ii) the 4 real case_ids subset — not the regressed-bucket count. The test docstring (lines 336-342) explicitly acknowledges that the cohort summary reads LATEST snapshot, not snapshot 1, and defers the snapshot-1-specific assertion to "a separate test below" that doesn't actually exist in this file. So the snapshot-1 healthy-cohort claim is asserted by presence-of-cases only, not by bucket distribution.

- **C (12/12)** — Forbidden-token audit is clean. Static grep of `git show 2632216 | grep -E "validated against|perforation completed|bullet-through-steel complete|validated physics|production ready|approved for service|ASME compliant"` returns **zero** hits in any code path; the same tokens only appear in the archived `.planning/phase12_audit_reports/C.md` text content (which cites them as the audit-token list, not as positive claims about the system). `signed off` returns **zero** hits in slice-D code or fixtures. The Tier 1 disclaimer trio (`"Tier 1 engineering candidate; not signed validation; not benchmark agreement"`) appears in the slice-D test module docstring (line 3) and is re-audited at test time across every snapshot manifest via `test_snapshot_envelope_carries_tier1_disclaimer_trio[0|1|2]` (parametrize over the 3-snapshot list, lines 513-526). The pinned assertions check `claim_tier == "Tier 1 engineering candidate"` exact + `"not_signed_validation"` substring + `"not_benchmark_agreement"` substring per snapshot manifest. The 5σ blueprint wording (which is statistically impossible at n=6) is handled honestly: the test docstring at lines 471-472 says "the 5σ language in the same paragraph is statistically impossible for n=6; we assert against the deliverables-list threshold instead" and the test uses `ANOMALY_SIGMA_INFO_MIN` (imported from `cohort_anomalies`, value 2.0) as the assertion threshold rather than gaming the number. Similarly the docstring at lines 355-363 on `test_snapshot3_cylinder_pv_extended_trust_score_visibly_degraded` documents that the trust score landing is around ~84 (still nominally "healthy" by bucket) and that the "regressed bucket" blueprint wording was aspirational against a deeper degradation than the fixture math achieves. **C:-8 substantively satisfied with bonus honesty credit**: the slice surfaces the gap between aspirational blueprint wording and statistical ceiling inside the test docstrings rather than padding the numbers or hiding the discrepancy.

- **A (7/8)** — Four of the five named anti-gaming guards from the blueprint sub-rubric §4.D are exercised distinctly. **M:-2** (named `SnapshotCaseInput.analysis_type` field; bucket+severity buckets via existing SSOT constants): the new dataclass field is named, the pass-through in `write_cohort_snapshot` is documented, and `COHORT_SNAPSHOT_MANIFEST_SCHEMA_VERSION` + `ANOMALY_SIGMA_INFO_MIN` + `TREND_SLOPE_INFO_MAX` are imported from their source modules in the test file (no inline literals for these). **T:-3** (each alarm payload's `case_id` + `axis` + `severity` pinnable distinctly): `test_z_score_outlier_alarm_payload_pins_case_axis_severity` (lines 480-505) and `test_trend_slope_alarm_fires_on_pv_extended` (lines 405-424) check the three fields independently per alarm; the cohort_count assertion (line 491) catches a future builder change that would lose cases. **C:-8** (Tier 1 disclaimer trio re-audited on every snapshot envelope): the parametrize at lines 513-526 fires the audit on all three snapshots; one missed snapshot trips one of the three parametrize cases. **E:-2** (no real-solver invocation; the three snapshots are generated in tmp_path from the Phase 12 C pre-baked fixtures; `reports/snapshots/` in the live repo is never touched): `_seed_repo` (lines 80-108) copies the four fixture dirs into `tmp_path/golden_samples/` and uses synthetic filler cases derived from `modal-cantilever-candidate`; `_write_three_snapshots` (lines 231-267) writes to `tmp_path/reports/snapshots/` via the `repo_root=tmp_path` kwarg on `write_cohort_snapshot`. The test imports `pathlib`, `json`, `shutil`, `datetime`, `pytest`, and pure-Python service modules only — no solver or LLM import. **One A-axis defect (-1)**: the severity-bucket name tuple `("info", "warn", "danger")` is hard-coded inline at three assertion sites (lines 424, 477, 505) rather than imported from a SSOT constant. The blueprint sub-rubric §4.D A:-1 guard text reads "bucket+severity buckets via existing SSOT constants" — the bucket-name set itself is the load-bearing SSOT here, not just the numeric thresholds. A future fourth bucket (e.g. "critical") would not surface in slice-D tests; the slice-C audit set a higher bar by importing every threshold from the source module that slice D didn't fully match for this one detail.

- **E (8/8)** — Full sweep `2171 passed, 7 skipped, 3 warnings in 22.59s`, no regressions vs slice-C baseline of 2157 + 7 skipped (+14 net additions, matching the 14 tests collected in `test_phase12_cohort_alarms.py` — exact accounting). `git show 2632216 --name-only | grep '^golden_samples/GS-[0-9]'` returns **empty** (signed registry untouched; HF1 forbidden-zone-as-named never approached). Slice D **does not touch any `golden_samples/` paths** at all — every fixture is read out of the existing `golden_samples/<case>-candidate/` subtrees seeded by Phase 11 A and Phase 12 C, and every write goes to `tmp_path`. No `HF1_GUARD_OVERRIDE` is set in the slice-D commit body, and the commit's name-only diff confirms only `.planning/phase12_audit_reports/C.md` (archive) + `backend/app/services/reporting/cohort_snapshot.py` + `backend/app/services/reporting/trust_score_timeline.py` + `tests/test_phase12_cohort_alarms.py` are touched — no `golden_samples/` paths, no `scripts/hf1_path_guard.py` modification, no override env var. No real CalculiX / OpenRadioss / LLM invocation in CI: the test file imports only `pathlib`, `json`, `shutil`, `datetime`, `pytest`, and `app.services.reporting.{cohort_snapshot,cohort_anomalies,cohort_executive_summary,cohort_trend_anomalies,trust_score_timeline,_schema_versions}` — all pure-Python report builders that read JSON. The two filler cases (`synthetic-cohort-bulk-01/02-candidate`) exist only in `tmp_path` per `_seed_repo` lines 92-107 and are explicitly documented as test-only background in the `FILLER_COHORT_CASES` docstring (lines 72-77). No defect — this is the strongest E-axis evidence across slices A/B/C/D so far (slice C had two demo-orchestrator latent bugs as the binding E-axis constraint; slice D has none).

- **V (7/8)** — TAA verdict APPROVE, with one coherence concern documented. (a) The blueprint §3.D step language calls for **"a `regressed` bucket on cylinder-pv-extended at snapshot 3"** and **"a 5σ z-score outlier on modal-stiff energy_audit"**. The shipped slice delivers (i) visible trust-score degradation on cylinder-pv-extended (`baseline_trust > latest_trust` with the exact magnitude documented as ~84, still nominally healthy by bucket) plus a trend-slope alarm firing at info-or-worse severity, and (ii) a z-score outlier at |z| ~= 2.24 (info severity) rather than 5σ. Both gaps are statistically determined: the bucket-threshold math at completeness ≥ 80 puts the degraded trust score at ~84 (above the healthy floor of 80), and the n=6 cohort caps |z| at ~sqrt(5)·sqrt(stdev_ratio) which lands around 2.24 for the engineered outlier. The slice handles this honestly — the commit body's "Test honesty note" paragraph names the gap explicitly ("the blueprint's wording is in tension with the statistical ceiling at small cohort size n"; "the test asserts the achievable signal ... and documents the gap inside the test docstrings rather than gaming the numbers"), and the test docstrings at lines 355-363 + 468-472 cite the same impossibility argument with the math. This is the correct engineering choice — surfacing the dashboard-visible signal (trend alarm + z-score outlier both firing) without gaming the magnitude — but a reviewer reading blueprint §3.D in isolation would expect the "regressed bucket" wording to be load-bearing. Mitigation: slice G retrospective should adopt one of (a) tighten the bucket thresholds (lower the healthy-floor to something the degradation actually crosses), (b) deepen the degradation injection (drop the energy_audit on cylinder-pv-extended too, not just on modal-stiff), or (c) revise the blueprint wording to match achievable statistical signals. (b) The two integration fixes folded into this slice (`SnapshotCaseInput.analysis_type` pass-through + `_convergence_weighted` modal/linear_static branches) are latent bugs that should arguably have been fixed inside slice A (where `_score_convergence_axis` got the same treatment) and slice C (where `cohort_snapshot.py` was first exercised with modal cases via the multi-case write path). The commit body acknowledges both as "Critical integration fixes folded into this slice" with a 4-line rationale per fix; this is transparent scope-broadening, not hidden. The trade is: slice D is slightly broader than the binding §3.D deliverable list, but the deliverable list is fully satisfied AND two real production bugs are closed in the same commit. Not load-bearing for APPROVE because (i) the deliverable list is fully satisfied, (ii) the integration fixes mirror existing trust_score.py logic line-for-line (the `_convergence_weighted` modal branch mirrors `_score_convergence_axis` modal branch from slice A, verified by source diff), (iii) the back-compat default `analysis_type="ballistic"` is preserved so every existing snapshot caller is untouched.

## Findings

- **MEDIUM** — The new `_convergence_weighted` modal and linear_static branches in `trust_score_timeline.py:198-232` have no direct unit test. They're exercised indirectly via the cohort snapshot tests (modal-cantilever fixtures hit the modal path; cylinder-pv-extended hits the linear_static path), but no test pins `_convergence_weighted({"convergence_kind": "modal", "mode_count_sweep": {"convergence_label": "candidate_observed_stable"}}) == 25` (or whatever the weighted floor is) directly. The Phase 12 A audit flagged the same gap on `_score_convergence_axis` in `trust_score.py` and slice A's gap is carried forward into slice D's twin scorer. A future change that breaks one branch (e.g. inverting the stable/unstable check on the modal path) would surface in the indirect tests as a trust-score number shift but not as a clearly-attributable assertion failure. Suggested fix: add a 6-line direct unit test in `tests/test_trust_score_timeline.py` (or a new `test_convergence_weighted.py`) that pins each of the four convergence_kind paths (explicit_dynamics 2-axis / linear_static mesh-only / modal mode_count-only / unknown-kind fallback) at the three stability labels (stable / unstable / inconclusive). Slice G retro carry-forward.

- **LOW** — Severity-bucket name tuple `("info", "warn", "danger")` is hard-coded inline at three assertion sites in `tests/test_phase12_cohort_alarms.py` (lines 424, 477, 505) rather than imported from a SSOT constant. The blueprint anti-gaming guard A:-1 reads "bucket+severity buckets via existing SSOT constants" — the bucket-name set is the load-bearing SSOT here, not just the numeric thresholds. Suggested fix: hoist a `SEVERITY_BUCKETS = ("info", "warn", "danger")` tuple into `backend/app/services/reporting/cohort_anomalies.py` and `cohort_trend_anomalies.py` (or a shared `severity_buckets.py`) and import it into the test file. Slice G retro carry-forward.

- **LOW** — `test_snapshot1_all_cases_healthy` (lines 327-344) does NOT actually assert snapshot 1's bucket distribution. The test docstring (lines 336-342) acknowledges that the cohort executive summary reads LATEST snapshot (snapshot 3 in this arc), and defers the snapshot-1-specific assertion to "a separate test below" that doesn't exist in this file. So the snapshot-1 healthy-cohort claim is asserted by presence-of-cases only, not by bucket distribution. The blueprint §3.D step 1 wording ("Snapshot 1: all 4 cases score full credit on every axis") is therefore not directly pinned by any single test — it's only implied by the absence of degradation injection in `_write_three_snapshots` for `snap_idx == 0`. Suggested fix: add `test_snapshot1_no_regressed_bucket` that constructs an executive summary from just the snapshot 1 manifest (or asserts the snapshot-1-specific completeness scorecard's total_score for each case is ≥ 80 on every case). Slice G retro carry-forward.

- **LOW** — The integration-fix scope broadening in this slice (`SnapshotCaseInput.analysis_type` pass-through + `_convergence_weighted` modal/linear_static branches) arguably should have been fixed inside slice A (where `_score_convergence_axis` got the analogous treatment) and slice C (where `cohort_snapshot.py` was first exercised with modal cases via the multi-case write path). The commit body is transparent about the scope broadening and the fix mirror-source ("Slice 12 A fixed `_score_convergence_axis` in trust_score.py but the timeline's parallel scorer was a separate code path"), so this is not a hidden scope creep. But the pattern — "latent bug surfaced by next slice exercising the full pipeline" — has recurred across A→B→C→D and suggests a parallel-pipeline-audit pass would surface more such gaps preemptively. Suggested fix: slice G retrospective should add a parallel-pipeline-audit checklist item — "for each new scorer/branch added in slice X, grep for parallel scorers in `trust_score_timeline.py`, `cohort_*.py`, `executive_summary.py` and verify the same dispatch logic is honored". Slice G retro carry-forward.

## Cumulative slice D axes

M + T + C + A + E + V = 11 + 13 + 12 + 7 + 8 + 7 = **58 / 63**

Per-axis floor check (slice-D sub-rubric):
* M 11 ≥ 10 ✓
* T 13 ≥ 10 ✓
* C 12 ≥ 10 ✓
* A 7 ≥ 6 ✓
* E 8 ≥ 7 ✓
* V 7 ≥ 7 ✓ (at floor — the "regressed bucket vs ~84 healthy trust score" + "5σ vs |z|~2.24" gaps are the binding constraints; both handled honestly via test-docstring documentation)

Verdict: **APPROVE**

All axes meet floor. Zero HIGH findings. One MEDIUM finding (no direct unit test on the new `_convergence_weighted` branches; carries forward the slice-A pattern). Three LOW findings, all suitable for slice-G retrospective carry-forward.

## Residual risks for next slices

1. **`_convergence_weighted` modal + linear_static branches need direct unit coverage** (MEDIUM): slice G retro should add a focused `test_convergence_weighted.py` that pins each of the four `convergence_kind` paths × three stability labels = 12 cases. The same gap exists on `_score_convergence_axis` from slice A; the retro should close both at once.
2. **Blueprint §3.D wording vs statistical ceiling** (V-axis concern): the "regressed bucket" + "5σ outlier" promises in the blueprint cannot be met at the cohort sizes / fixture data used in slice D. Slice G retrospective should pick one of three closures: (a) tighten the bucket thresholds, (b) deepen the degradation injection, or (c) revise the blueprint wording. The current state — honest documentation of the gap in test docstrings — is sufficient for slice-D approval but should not become a permanent pattern.
3. **Severity-bucket SSOT** (LOW): hoist `SEVERITY_BUCKETS = ("info", "warn", "danger")` into a single source module and import it across the cohort anomaly tests + the alarm builders themselves. A future fourth bucket (e.g. "critical") would otherwise fragment across multiple inline-literal sites.
4. **Snapshot-1 healthy-cohort claim is presence-only** (LOW): `test_snapshot1_all_cases_healthy` does not actually assert snapshot 1's bucket distribution because the executive summary reads LATEST. Add a snapshot-1-specific test that pins the per-case completeness scorecard total directly.
5. **Parallel-pipeline-audit checklist** (LOW): the pattern "slice X adds a scorer, slice X+1 surfaces the parallel scorer's latent bug" has now recurred A→B→C→D twice (slice A added `_score_convergence_axis`, slice D fixed `_convergence_weighted`; slice C exercised `cohort_snapshot.py` with modal cases, slice D added the `analysis_type` pass-through). Slice G retrospective should adopt a preemptive parallel-pipeline grep as part of every "new scorer/branch" addition.
6. **Slice C MEDIUMs carried forward**: the two demo-orchestrator runtime bugs flagged in slice C's TAA report (`demo_modal_cantilever_pv` relative-import + `demo_cylinder_pv_extended` missing `run_pipeline` symbol) are **NOT** fixed in slice D. The commit body and the slice-D scope did not include orchestrator repairs; CI continues to never invoke these paths so the bugs remain silent. Slice D's "Critical integration fixes" section addresses the cohort writer / trust-score timeline only. The orchestrator fixes are still queued to slice E/F per the slice-C residual-risk list.
7. **Slice B `unreadable` branch carry-forward still open** (carried from slice C): `_score_modal_specific_axes`'s `except (json.JSONDecodeError, OSError)` branch in `case_completeness.py` has no direct test coverage. Slice D did not close this gap.
