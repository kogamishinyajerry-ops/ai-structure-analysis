# Phase 12 E — TAA report

**Slice commit:** `82fcb71`
**Verdict:** APPROVE
**Test count:** 20 tests collected in `frontend/test/cohortDashboardClient.test.tsx` (3 SSOT tuple pins + 2× pass-through + 2× unknown fallback for `defensiveBucket` + 2× pass-through + unknown-fallback-not-danger for `defensiveSeverity` + 2× for `defensiveAlertKind` + 2× `bucketColor` + 2× `severityColor` + 6 `fetchCohortDashboardViewModel` builder tests + 1 Tier 1 disclaimer trio audit; reconciles exactly with `vitest run` output `Tests 20 passed (20)`)
**Test pass:** 20/20 targeted; full frontend sweep `12 test files passed; 117 passed (117)` — matches commit-body claim "+20 vs prior 97 = 117" exactly, zero regressions
**Schema bumps:** none — slice E is a pure frontend orchestration layer; no `schema_version` constants change, no backend touched (full backend sweep referenced in commit body as `2171 passed, 7 skipped` is a clean carry-forward from slice D's 2171/7 baseline)

## Per-axis evidence

- **M (11/12)** — SSOT discipline is strong. Three named tuple constants `DASHBOARD_BUCKETS`, `DASHBOARD_SEVERITIES`, `DASHBOARD_ALERT_KINDS` (`cohortDashboardClient.ts:45-67`) are declared `as const` with their `(typeof X)[number]` type aliases derived in lockstep, then re-pinned by three dedicated tuple-shape tests (`DASHBOARD_BUCKETS SSOT` / `DASHBOARD_SEVERITIES SSOT` / `DASHBOARD_ALERT_KINDS SSOT`, lines 38-64 of the test file) — a future PR adding a fifth bucket / severity / alert kind would trip the `toEqual([...])` literal-array assertion. The defensive parser triad (`defensiveBucket` / `defensiveSeverity` / `defensiveAlertKind`, lines 73-95) each route every undocumented value to the literal `"unknown"` sentinel, the only out-of-band string in the SSOT tuple. Color tokens `bucketColor` / `severityColor` (lines 101-113) map each documented enum value to a distinct CSS variable and route `'unknown'` to `'var(--text-secondary)'` (neutral gray) — distinctness pinned by the `Set` size assertion in `bucketColor`/`severityColor` tests (lines 119-126, 134-141). The view-model interface `CohortDashboardViewModel` (lines 142-149) carries the named `source: 'live' | 'partial-fallback' | 'fallback'` 3-tuple as a closed union — degradation modes cannot quietly grow a fourth value. The named `schemaVersions: SchemaFootprint` field (lines 136-140, 145) is the explicit anchor for the Phase 12 retro §3 "four schema versions in dashboard footer" carry-forward, named in commit-body §"What" bullet 6. **One M-axis defect (-1)**: the `bucketColor` / `severityColor` color tokens hard-code the hex fallbacks `'#0a8a4a'` / `'#b8860b'` / `'#c0392b'` inline at three sites each (lines 102-104 + 109-111) rather than importing a shared `THEME_TOKENS` SSOT. The same hex triple already exists in `cohortExecutiveSummaryClient.ts` and elsewhere; a future theme migration to a Tailwind variable namespace would need to find every hex inline (this is a slice-G carry-forward, not load-bearing for slice E).

- **T (14/15)** — Test coverage is broad and the test docstrings explicitly call out the X:-2 load-bearing rule. T:-3 evidence: each defensive-parser branch has a distinct test pair (pass-through for the 3 documented values + fallback for at least 3 distinct unknown classes — `undefined`, empty string, typo, ALLCAPS case-mismatch). The load-bearing anti-promotion guard is asserted explicitly at line 97: `expect(defensiveSeverity('catastrophic')).not.toBe('danger')` — this catches a regression where a future hypothetical "more severe than danger" enum value gets coerced into `'danger'` instead of `'unknown'`. The `bucketColor`-for-unknown anti-promotion is similarly pinned by `expect(unknownColor).not.toBe(bucketColor('regressed'))` (line 129) — the neutral-gray sentinel must NOT collide with the danger-red token. The three degradation modes (live / partial-fallback / fallback) get one test each (lines 234-276) with distinct stub-fetch sequences; `partial-fallback` asserts the surviving surfaces still populate + the failed surface contributes zero rows (`trendAlerts).toEqual([])`, line 258). The two future-schema-drift surfacing tests (lines 278-315) inject `bucket: 'panicking'` and `severity: 'catastrophic'` into the EXEC/ANOM payloads and assert the view model surfaces `'unknown'` rather than the upstream client's most-conservative coercion (`regressed` / `info`) — these are the load-bearing X:-2 evidence tests. The bucket-counts test (lines 317-341) pins the sum-equals-cohort-size invariant. The Tier 1 disclaimer audit (lines 348-358) iterates every alert and pins the case_id + axis + bucketed severity trio. **One T-axis defect (-1)**: no test exercises the `fetchRaw` network error path (the `catch (err)` branch at `cohortDashboardClient.ts:276-281` returning `{ raw: null, error: ... }` from an Error instance). The 5xx-error path is tested via `{ status: 500 }` stubs but the actual `fetch` throws (DNS failure, AbortError, network-disconnect) only exercise the same downstream degradation logic indirectly. A 6-line stub that has `global.fetch = vi.fn(async () => { throw new TypeError('network error') })` would close this gap. Slice G retro carry-forward.

- **C (12/12)** — Forbidden-token audit is clean. `git show 82fcb71 -- frontend/src/cohortDashboardClient.ts frontend/test/cohortDashboardClient.test.tsx | grep -iE 'production-ready|signed validation|benchmark agreement|enterprise-grade|certified|battle-tested|gold standard|seamless|guarantee|flawless|robust|state-of-the-art'` returns **only** three hits, all of which are NEGATIONS inside the Tier 1 disclaimer trio (`"Tier 1 engineering candidate; not signed validation; not benchmark agreement"` at the module docstrings of both new files). No positive forbidden token survives on the slice-E diff. The disclaimer trio is preserved verbatim in BOTH new files' top docstrings (`cohortDashboardClient.ts:3` + `cohortDashboardClient.test.tsx:3`) — a slice-E reader who only sees one file still encounters the trio. The Tier 1 envelope on every alert payload is re-pinned at test time via the `for (const alert of view.alerts)` audit loop (lines 352-357) that requires each alert's `caseId` non-empty string + `axis` non-empty string + `severity` ∈ `DASHBOARD_SEVERITIES` (a 4-tuple that already includes `'unknown'`). The fixture payloads `EXEC_OK` / `ANOM_OK` / `TREND_OK` (lines 170-231) carry the literal `claim_tier: 'Tier 1 engineering candidate'` + `claim_boundary: 'tier1_engineering_candidate; not_signed_validation; not_benchmark_agreement'` strings — even though no test currently extracts these fields, they propagate as part of `EXEC_OK`'s identity through the spread-injection drift tests (lines 280-308) so a future audit can add an envelope-trio pin without changing fixture structure. **C:-8 substantively satisfied.**

- **A (7/8)** — Four of the five named anti-gaming guards from blueprint sub-rubric §4.E are exercised distinctly. **M:-2** (three named SSOT tuples + defensive parsers per tuple + bucketColor/severityColor over closed enum types): the three tuple-shape pins (lines 38-64) + the three parser triads + the two color-function tests are all distinct. **T:-3** (every parser fallback path tested independently — known value pass-through + every distinct unknown class): the pass-through tests cover the documented enum exhaustively (3/3 for buckets, 3/3 for severities, 3/3 for alert kinds), and the unknown-class tests cover at least 3 distinct unknown forms per parser. **X:-2** (the load-bearing slice-E guard): the explicit `.not.toBe('danger')` assertion at line 97 + the `.not.toBe(bucketColor('regressed'))` assertion at line 129 + the `expect(c.bucket).toBe('unknown')` after injecting `bucket: 'panicking'` at line 294 + the `expect(drift?.severity).toBe('unknown')` after injecting `severity: 'catastrophic'` at line 314 are FOUR independent anti-promotion guards on the X axis, each catching a distinct upstream coercion pattern. Verified by source-grep: `cohortExecutiveSummaryClient.ts:52-55` does fall back to `'regressed'`; `cohortAnomaliesClient.ts:50` falls back to `'info'`; `cohortTrendAnomaliesClient.ts:57-65` falls back to `'info'` — slice E's defensive layer DOES invert these to `'unknown'`, the engineering coherence claim holds. **C:-8** (Tier 1 disclaimer trio preserved in both new files' docstrings AND envelope-shape audit on every alert): the for-loop at lines 352-357 catches a future builder that drops `caseId` / `axis` / severity-out-of-enum. **E:-2** (three degradation modes pinned via separate tests so a future PR cannot silently lose `partial-fallback`): the `expect(view.source).toBe('live')` / `'partial-fallback'` / `'fallback'` assertions at lines 237, 254, 268 are three distinct test bodies — losing the middle mode trips one test, not all three. **One A-axis defect (-1)**: the spread-injection fixture drift tests (lines 278-315) hard-code `'panicking'` and `'catastrophic'` as the future-schema drift strings rather than parametrizing over a list of e.g. `['panicking', 'imploding', 'CHAOTIC']` (multiple drift classes simultaneously). A future schema-drift that lands with multiple unknown values across the same payload (e.g. drift bucket + drift severity + drift alert_kind in one anomaly row) would surface inside slice-E's tests as 3 separate single-drift tests rather than one joint-drift test. Mitigated because each parser is independent and the unit-level fallback tests cover the orthogonal case set, but slice C/D's pattern of parametrized assertion-over-tuple set a higher bar that slice E didn't fully match here.

- **E (8/8)** — Full frontend sweep `12 test files passed; 117 passed (117) in 1.67s`, zero regressions vs slice-D baseline of 97 passed (+20 net additions, matching the 20 collected tests exactly). `git show 82fcb71 --stat` confirms **only** `.planning/phase12_audit_reports/D.md` (archive) + `frontend/src/cohortDashboardClient.ts` (new) + `frontend/test/cohortDashboardClient.test.tsx` (new) are touched — **no** `golden_samples/GS-\d{3}/` paths, **no** `scripts/hf1_path_guard.py` modification, **no** `HF1_GUARD_OVERRIDE` env var in the commit body. No real backend / solver / LLM invocation in the new tests: the test file imports only `vitest` + the new module under test; all three network surfaces are stubbed via `global.fetch = vi.fn(...)` (`stubFetchSequence` at lines 153-167) — no real `fetch()` to `localhost` or external. The `tmp_path` discipline is N/A for frontend (no filesystem writes), and the stub-fetch queue is reset per test via `stubFetchSequence([...])` re-assigning `global.fetch` (no cross-test leakage). TypeScript strict-mode clean on the new module (the `as DashboardAlertKind` annotations at lines 208 + 229 are the only narrowing casts and they are tied to literal strings from the SSOT tuple, not arbitrary user data). The raw-JSON-fetch path at lines 264-282 owns its own parsing layer — explicitly to circumvent the upstream clients' coercion to `'regressed'` / `'info'` — and this design choice is documented in the module docstring (lines 33-38) + commit body §"What" bullet 4. Three degradation modes (`live` / `partial-fallback` / `fallback`) honored by the `liveCount` counter (lines 299-316) with three separate test cases for the boundary conditions. No demo orchestrator / no integration-fix folding / no slice-D scope-broadening — slice E ships exactly what its blueprint §3.E names plus the named visual-deferral. No defect.

- **V (7/8)** — TAA verdict APPROVE, with one coherence concern documented and the visual deferral judged HONEST. The commit body §"DEFERRED to slice G retrospective" paragraph names the visual restructure explicitly: *"Visual restructure of CohortDashboardPanel.tsx layout (4-case left rail + center timeline + right alerts + footer) to blueprint #07 fidelity is heavier UI work that needs a running dev server to verify the rendering. Slice E delivers the load-bearing data layer + defensive parsers; the visual layout is left as a Phase 12 G carry-forward."* Verified by `git log --oneline 82fcb71 -- frontend/src/components/CohortDashboardPanel.tsx` → only hit is the much-earlier Phase 4 commit `7f0a52a`; `CohortDashboardPanel.tsx` is **NOT** touched in this slice. Two competing readings of this scoping decision: (a) **HONEST**: the data layer ships in clean isolation; the visual is named as DEFERRED at the right granularity (one slice forward, slice G); the deferred work is concretely scoped ("4-case left rail + center timeline + right alerts + footer"); the data layer is functionally complete in the sense that any consumer (current `CohortDashboardPanel.tsx` or a slice-G replacement) gets the defensive view model — this is correct decomposition for a Tier 1 candidate where running-dev-server visual verification is a separate skill from defensive-parser unit testing. (b) **UNDER-DELIVERY**: blueprint §3.E (referenced as binding in the commit body §"What" first line) does promise "blueprint-#07-shaped view model" — the view model exists but #07-fidelity is the rendered-pixel layout, which is precisely what's deferred. Reviewers who read the commit subject line "cohort dashboard client orchestrator + defensive parsers" and the slice-E sub-rubric (M/T/X/C/A/E/V floors) without descending into the §"DEFERRED" paragraph would reasonably believe the dashboard is wired end-to-end. Resolution: the named deferral in the commit body + the explicit naming of "blueprint #07 fidelity" as the deferred deliverable (not as the shipped deliverable) + the named slice-G target qualifies this as honest scoping rather than hidden under-delivery — but the sub-rubric V floor of ≥7/8 is the right calibration for "honest deferral of a named load-bearing visual deliverable", not 8/8. A 6/8 would imply hidden gaming, which is not what happened. A clean 8/8 would require the visual layer to actually ship in slice E. 7/8 is at floor and load-bearing. The slice-G retrospective should treat the visual restructure as a hard binding deliverable, not a soft carry-forward.

## Findings

- **MEDIUM** — The blueprint #07 visual-restructure of `frontend/src/components/CohortDashboardPanel.tsx` (4-case left rail + center timeline + right alerts + footer with the 4 schema versions) is DEFERRED to slice G. Slice E ships the data layer (defensive parsers + view-model builder + three degradation modes + schema-version footprint object) but the rendered UI that surfaces neutral-gray unknown buckets to a reviewer's eye is not in this slice. **Impact**: a reviewer running the frontend dev server today will still see the Phase 4-era `CohortDashboardPanel.tsx` layout, which consumes `cohortExecutiveSummaryClient`'s `parseBucket` (the one that coerces unknown values to `'regressed'`) — NOT the new `defensiveBucket` from slice E. The slice-E view model is reachable only by future code; existing consumers don't switch over. The defensive-parsing benefit is therefore latent until slice G wires the panel to `fetchCohortDashboardViewModel`. **Suggested fix for slice G**: (a) port `CohortDashboardPanel.tsx` to consume `fetchCohortDashboardViewModel` instead of the three upstream clients; (b) render bucket badges with `bucketColor(case.bucket)` so the neutral-gray sentinel actually appears in the UI; (c) add a footer row that prints the three `schemaVersions.*` fields so the Phase 12 retro §3 "4 schema versions in dashboard footer" goal is finally pinned. Without the panel-side migration, slice E's X:-2 guard is correct in code but invisible in the running product. Carries forward as the load-bearing slice-G deliverable, not as a soft retro item.

- **LOW** — The `fetchRaw` helper at `cohortDashboardClient.ts:264-282` has its `catch (err)` branch (lines 276-281) covered only by the `await res.json()` path; the `await fetch()` throwing (network failure / AbortError / DNS error / TypeError) is not exercised by any of the 6 view-model builder tests (all of which use `{ status: 500 }` stubs that resolve, not throw). A 6-line test that stubs `global.fetch` to throw a `TypeError('network error')` and asserts the view model degrades to `fallback` with the error message captured would close this gap. Suggested fix: add `test_fetchCohortDashboardViewModel_handles_network_throw` to `cohortDashboardClient.test.tsx`. Slice G retro carry-forward.

- **LOW** — The color tokens `bucketColor` / `severityColor` hard-code the hex fallbacks `'#0a8a4a'` / `'#b8860b'` / `'#c0392b'` inline at three sites each (lines 102-104 + 109-111), and the CSS variable names `'var(--accent)'` / `'var(--text-warning)'` / `'var(--danger)'` / `'var(--text-secondary)'` are not centralized in a shared theme tokens module. The same hex triple already exists in `cohortExecutiveSummaryClient.ts` (via the `bucketTone` function plus its consumer) and inline elsewhere. A future theme migration (e.g. to a Tailwind palette or to a single `theme.ts` exporter) would need to find every inline hex. Suggested fix: hoist a `THEME_TOKENS` object into a new `frontend/src/theme.ts` and reference it across the cohort client family. Slice G retro carry-forward.

- **LOW** — The fixture-drift tests (`surfaces unknown bucket on backend drift` at lines 278-295 and `surfaces unknown severity on backend drift` at lines 297-315) each inject one drift class at a time. A composite-drift test that injects `bucket: 'panicking'` + `severity: 'catastrophic'` + `kind: 'future_alarm_kind'` simultaneously in one payload would pin the orthogonality of the three defensive parsers as a single joint-test. The current 3 single-drift tests cover the same surface area, so this is purely structural-readability rather than a coverage gap. Slice G retro carry-forward.

## Cumulative slice E axes

M + T + C + A + E + V = 11 + 14 + 12 + 7 + 8 + 7 = **59 / 63**

Per-axis floor check (slice-E sub-rubric):
* M 11 ≥ 10 ✓
* T 14 ≥ 9 ✓ (also ≥ 10-test minimum: 20 actual tests)
* X (load-bearing X:-2 anti-gaming guard verified across 4 independent assertion sites — defensiveBucket NOT promoted to 'regressed', defensiveSeverity NOT promoted to 'danger', bucketColor unknown NOT colliding with danger token, severityColor unknown NOT colliding with danger token; floor implicitly cleared via T+A scoring of the X-axis evidence)
* C 12 ≥ 10 ✓
* A 7 ≥ 6 ✓
* E 8 ≥ 7 ✓
* V 7 ≥ 7 ✓ (at floor — the load-bearing constraint is the visual-deferral honesty; named deferral with concrete slice-G target qualifies as honest scoping, not hidden under-delivery)

Verdict: **APPROVE**

All axes meet floor. Zero HIGH findings. One MEDIUM finding (CohortDashboardPanel.tsx visual restructure deferred to slice G — load-bearing for slice G, not load-bearing for slice E approval since the deferral is explicit and named). Three LOW findings, all suitable for slice-G retrospective carry-forward.

## Residual risks for next slices

1. **Blueprint #07 visual restructure is now slice G's load-bearing deliverable** (MEDIUM): the data layer ships in slice E, but the reviewer-facing pixel layout that surfaces neutral-gray unknown buckets / alarm severity color tokens / 4-schema-version footer is deferred. Slice G must port `CohortDashboardPanel.tsx` to consume `fetchCohortDashboardViewModel` AND render `bucketColor(case.bucket)` AND render the `schemaVersions.*` footer triple AND the three degradation-mode banners (`'live'` / `'partial-fallback'` / `'fallback'`). Without this, slice E's X:-2 guard is correct in code but invisible in the running product.
2. **Slice E network-throw branch coverage gap** (LOW): the `fetchRaw` `catch (err)` branch is exercised only by JSON-decode errors via the `{ status: 500 }` stub path; real network-throw cases (TypeError, AbortError) aren't pinned by any test. Slice G should add a stubbed-throw test.
3. **Theme tokens fragmentation** (LOW): hex color fallbacks live inline in `cohortDashboardClient.ts` and several other cohort clients. A centralized `frontend/src/theme.ts` exporter would future-proof the theme migration.
4. **Composite-drift test missing** (LOW): the spread-injection drift tests cover one drift class at a time; a joint-drift composite test would pin parser orthogonality more tightly.
5. **Slice D MEDIUM carried forward** (from slice D residual risks): the `_convergence_weighted` modal+linear_static direct unit-test gap (currently exercised indirectly via cohort timeline tests) is still open. Slice E did not touch backend; slice G or a dedicated backend slice should close this.
6. **Slice D LOWs carried forward**: severity-bucket SSOT (hoist `SEVERITY_BUCKETS` into a single source module), snapshot-1-bucket-distribution direct pin, parallel-pipeline-audit checklist — all three remain open after slice E. Slice G retrospective should batch-close these.
7. **Upstream clients still coerce to most-conservative values** (carry-forward design choice): `cohortExecutiveSummaryClient.ts:54` still returns `'regressed'` for unknown buckets; `cohortAnomaliesClient.ts:50` / `cohortTrendAnomaliesClient.ts:57` still return `'info'` for unknown severities. Slice E deliberately routes around these via raw-JSON fetch, which is the correct short-term move, but it leaves two parser-fallback patterns in the codebase. A future slice could either (a) unify by adding `getRawBucket()` / `getRawSeverity()` accessors on the upstream clients, OR (b) deprecate the upstream clients in favor of the dashboard's parser as canonical. The current dual-pattern is a known carry-forward, not a defect.
