# Phase 12 B — TAA report

**Slice commit:** `7e278aa`
**Verdict:** APPROVE
**Test count:** 33 (31 distinct test functions; 1× parametrize over 3-tuple of (axis_name, token) adds 2 collected cases; full/half/zero boundary triplets account for 12 of the per-axis tests)
**Test pass:** 33/33; full sweep 2122 passed + 7 skipped + 3 warnings (slice-A baseline was 2090 passed → +32 net, no regressions; matches commit-body verification claim)
**Schema bumps:** `CASE_COMPLETENESS_SCHEMA_VERSION` 1.1.0 → 1.2.0 ✓ (`_schema_versions.py:81-109` carries full bump-history docstring naming the four new modal-specific axes and the graceful-degrade contract for 1.1.0-era payloads). Centralized parametrized pin in `tests/test_schema_versions_stamping.py:271` updated. The Phase-11-era duplicate literal pin in `tests/test_phase11_analysis_type_rubric.py:65-75` was retired and replaced with a `payload["schema_version"].startswith("1.")` family check (one SSOT remains).

## Per-axis evidence

- **M (12/12)** — SSOT discipline is exemplary. The four new modal-specific weights are named module-level constants with docstrings citing both the JSON path read and the methodology-doc anchor: `WEIGHT_MODE_COUNT_COVERAGE = 15` (`case_completeness.py:119-124`), `WEIGHT_FREQ_CONVERGENCE = 15` (lines 125-128), `WEIGHT_MODE_SHAPE_QUALITY = 10` (lines 129-132), `WEIGHT_MASS_PARTICIPATION = 10` (lines 133-136). The five modal-universal axes (`WEIGHT_STARTER_DECK_MODAL` / `WEIGHT_ENGINE_DECK_MODAL` / `WEIGHT_BALLISTIC_METRICS_MODAL` / `WEIGHT_ENERGY_AUDIT_CLOSED_MODAL` / `WEIGHT_CONVERGENCE_STABLE_MODAL`, lines 146-167) each carry a docstring explaining the reweight rationale ("reduced from 20 ballistic default to 10 so the four modal-specific axes can sum to 50"). Methodology SSOT (`analysis_type_completeness_rubric.md` §modal — substantiated rubric) cites every named Python identifier with a Concept→Symbol→Source table and includes a math check (5×10 + 50 = 100) that mirrors the import-time `_assert_rubric_weights_consistent` guard. Slice-A LOW for literal `"1.2.0"` in `parse_modal_dat` is **closed** (`modal_extraction.py:259-262`: imports `CONVERGENCE_STUDY_SCHEMA_VERSION` and stamps the SSOT constant with a comment naming the slice-A finding it closes). Lockstep enforcement is unbroken.

- **T (14/15)** — Per-boundary coverage is comprehensive. T:-3 boundary discipline: every modal-specific axis has full/half/zero triplet at the documented thresholds (mode_count_coverage 80%/50%: lines 186-228; freq_convergence 1%/5%: lines 232-274; mode_shape_quality 0.95/0.80: lines 278-320; mass_participation 50%/20%: lines 324-366). T:-5 named-advisor-concern discipline: four named concerns asserted independently (MAC line 432, Lanczos line 439, mass participation line 445, frequency tolerance line 456) + parametrized "modal convergence kind" indicator across all three axis names (line 642-651). Slice-A MEDIUM closure: 4 trust_score modal-branch behavioral tests (stable→100 / unstable→30 / inconclusive→0 / absent→0, lines 534-571) pinning `raw_score` + rationale substrings ("modal convergence_kind" / "mode_count_sweep stable|unstable|inconclusive" / "mesh_sweep and dt_sweep N/A") — exactly the pattern the slice-A audit called for. Cross-cutting tests: rubric sum=100, axis-keys-match-substantiation (lines 90-110 asserts both the 9 expected keys AND the absence of dropped ballistic optional-artifacts), modal-axes-absent graceful-degrade (lines 370-387 — confirms 1.1.0-era payload contract), happy-path 100/100 (lines 390-406), envelope disclaimer-trio post-bump (lines 579-598), modal-universal axes use modal-weight caps not ballistic-inherited caps (lines 601-626). **One T-axis defect (-1)**: the slice-B modal advisor branch carries four named concerns surfaced into three different list fields (`mesh_quality_concerns` / `boundary_condition_questions` / `failure_modes_to_consider`), and the four-named-concerns coverage is complete; **however** the modal-axes-absent test asserts `score.score == 50` only — it does not assert each absent axis's `evidence_status` field equals `"unreadable"` on the `metrics_path.read_text` failure path (`case_completeness.py:867-898`, an entire `except (json.JSONDecodeError, OSError)` branch with four unreachable `evidence_status="unreadable"` entries). The `unreadable` branch is shipped code with zero direct test coverage. Mitigated by being a small parallel of the `absent` branch (same shape, different status string), but it is a real anti-gaming-guard gap vs the slice-A pattern that exercised every named refusal path.

- **C (12/12)** — Forbidden-token grep clean across the full commit. `git show 7e278aa | grep -nE "validated against|perforation completed|bullet-through-steel complete|validated physics|production ready|certified[^_]|approved for service|ASME compliant|signed off"` returns **one hit** on diff line 224 which is the archived slice-A audit report quoting the forbidden-token list itself (not a positive claim). "signed validation" appears only in the disclaimer-negation form `"not signed validation"` at lines 7, 844, 900 (commit body + advisor-critique module docstring + test file docstring), as required by the Tier 1 disclaimer trio contract. The `convergence_kind == "modal"` advisor branch text (`advisor_critique.py:286-348`) uses "Tier 1 candidate" disclaimer language explicitly ("refine before reporting the natural frequency as a Tier 1 candidate"). The methodology doc §modal section uses "engineering-practice floor" / "ASCE 7 / Eurocode 8 engineering-practice floor" — both established normative anchors, not positive validation claims. `test_modal_completeness_envelope_still_carries_disclaimer_trio` (lines 579-598) confirms `claim_tier == "Tier 1 engineering candidate"` + `claim_boundary` contains `"not_signed_validation"` + `"not_benchmark_agreement"` after the 1.2.0 schema bump. C:-8 substantively satisfied.

- **A (8/8)** — All six named slice-B anti-gaming guards exercised distinctly and substantively. **M:-2** (named constants + sum boundary): four new modal-specific constants + five renamed universal constants, all pinned by tests 61 / 71 / 81 / 90; sum=100 enforced at import via `_assert_rubric_weights_consistent` (`case_completeness.py:241-268`) and re-pinned by test 81. **T:-3** (per-partial-credit boundary): full/half/zero triplets on each of the four modal-specific axes (12 tests, lines 186-366). **T:-5** (each named advisor concern independently): four named concerns each have a dedicated test (432 MAC / 439 Lanczos / 445 mass participation / 456 frequency tolerance) + parametrize over the modal-indicator token across three axis names (lines 634-651). **C:-8** (Tier 1 disclaimer trio + no forbidden tokens after bump): re-audited by test_modal_completeness_envelope_still_carries_disclaimer_trio (lines 579-598) and forbidden-token grep above. **A:-6** (AdvisorContext.extra consumed to produced concern, closes Phase 11 retro §2): `advisor_critique.py:321` reads `context.extra.get("mode_count_target")`; presence-vs-absence branches in lines 322-344 both surface; test_modal_advisor_reads_mode_count_target_from_context_extra (line 465) asserts `"12 extracted modes"` appears in the produced concern when `extra={"mode_count_target": 12}`; test_modal_advisor_surfaces_mass_participation_question (line 445) asserts the absence-branch fallback prompt. **E:-2** (schema bump + bump-history docstring + central baseline test pin + slice-A LOW for literal version string closed): _schema_versions.py:81-109 bump-history present; tests/test_schema_versions_stamping.py:271 central pin updated; modal_extraction.py:259-262 closes slice-A LOW. Six guards × six substantive exercises = full A axis.

- **E (8/8)** — Full sweep `2122 passed, 7 skipped, 3 warnings in 17.38s` — no regressions vs slice-A baseline of 2090 + 7 skipped (+32 net additions, matching the +33 collected in test_phase12_modal_advisor.py minus 1 retired pin from test_phase11_analysis_type_rubric.py — exact accounting). `git diff 7e278aa^ 7e278aa -- golden_samples/` returns empty (signed registry untouched). HF1 grep against the three slice-B-touched code files returns zero hits (forbidden zone untouched). `git show 7e278aa --name-only --format=""` shows 9 files changed, every entry maps to a §3.B blueprint deliverable: case_completeness.py (modal-specific scorer), advisor_critique.py (modal branch), _schema_versions.py (1.2.0 bump-history), modal_extraction.py (slice-A LOW closure), methodology doc (§modal substantiated rubric section), test_phase12_modal_advisor.py (33 tests), test_phase11_analysis_type_rubric.py (retire duplicate pin), test_schema_versions_stamping.py (central pin update), .planning/phase12_audit_reports/A.md (archive). Slice-A MEDIUM (trust_score modal-branch behavioral tests) **closed**. Slice-A LOW (literal "1.2.0") **closed**. Two slice-A LOWs carried forward to slice-G retro remain open as scheduled. No conflicts with the four-question gate.

- **V (7/8)** — TAA verdict APPROVE, with one coherence concern documented. (a) The bump-history docstring in `_schema_versions.py:100-102` states "animation_manifest and result_mesh remain inside the modal universal axes at reduced weight; notes was dropped" — but the actual rubric (`case_completeness.py:213-229` and asserted by `test_modal_rubric_axis_keys_match_substantiation` lines 107-110) drops `animation_manifest` AND `result_mesh` AND `notes` AND `generator_script` entirely from the modal rubric. The commit body and methodology doc state the correct disposition ("dropped"); only the schema_versions bump-history docstring is inaccurate. This is a documentation-vs-code drift on a load-bearing SSOT file (the bump-history is the one place a future maintainer reads to understand the 1.1.0→1.2.0 contract change). Mitigated by the test (line 107) catching any code-side drift, but the docstring would mislead a maintainer reading it in isolation. Not load-bearing for APPROVE because (i) the methodology doc and commit body are accurate, (ii) the test enforces the correct shape, (iii) the docstring inaccuracy is a one-line phrasing fix; but it is a real documentation finding worth slice-G retro carry-forward.

## Findings

- **LOW** — `_schema_versions.py:100-102` bump-history docstring says "animation_manifest and result_mesh remain inside the modal universal axes at reduced weight; notes was dropped" but the actual modal rubric drops all four optional artifacts (animation_manifest / result_mesh / notes / generator_script). The methodology doc §modal section ("Dropped from modal: animation_manifest / result_mesh / notes ... generator_script is also dropped") and the commit body get this right; only the schema bump-history docstring is inaccurate. Suggested fix: one-line edit to align the bump-history with the rubric. Slice-G retro carry-forward at minimum.

- **LOW** — `_score_modal_specific_axes`'s `except (json.JSONDecodeError, OSError)` branch (`case_completeness.py:867-898`) ships four `evidence_status="unreadable"` entries that are not directly tested. The `absent` branch (when `metrics_path is None or not metrics_path.is_file()`) is exercised by `test_modal_axes_absent_when_modal_summary_missing`, but the unreadable-JSON branch is shipped behavior with zero direct test coverage. Suggested fix: one test writing an intentionally malformed JSON file to the metrics path and asserting `entry.evidence_status == "unreadable"` on each of the four axes. Not a slice-B blocker because the failure mode is symmetric with the `absent` branch (same return shape, different status string) and the wider exception handling pattern is established in the codebase.

- **LOW** — The Phase 12 B blueprint §3.B is not visible in this audit's checkout (the binding sub-rubric is in this audit briefing, not the blueprint file). All four named anti-gaming guards from the briefing are exercised, and the modal-specific rubric mirrors the four named advisor concerns 1:1 (the explicit slice-B coherence claim). Slice-G retro should re-check the blueprint text against the shipped rubric to confirm no axis name drift.

## Cumulative slice B axes

M + T + C + A + E + V = 12 + 14 + 12 + 8 + 8 + 7 = **61 / 63**

Per-axis floor check (slice-B sub-rubric):
* M 12 ≥ 10 ✓
* T 14 ≥ 12 ✓
* C 12 ≥ 10 ✓
* A 8 ≥ 6 ✓
* E 8 ≥ 7 ✓
* V 7 ≥ 7 ✓ (at floor — the schema-versions bump-history docstring drift is the binding constraint)

Verdict: **APPROVE**

All axes meet floor. No HIGH findings. No MEDIUM findings. Three LOW findings, all suitable for slice-G retrospective carry-forward (none blocks slice C).

## Residual risks for next slices

1. **Slice-G retro must edit `_schema_versions.py` bump-history** to align with the methodology doc and commit body (animation_manifest + result_mesh + notes + generator_script all dropped from modal, not just notes). One-line fix.
2. **`unreadable` branch of `_score_modal_specific_axes` lacks direct test coverage** (mirror of the slice-A pattern that exercised every named refusal path). Add a malformed-JSON test in slice C or slice G.
3. **Slice-A LOWs all closed in this slice** — literal "1.2.0" in parse_modal_dat fixed at modal_extraction.py:259-262; trust_score modal-branch behavioral tests added at test_phase12_modal_advisor.py:534-571. Two slice-A LOWs that remain are (a) parse_modal_frd blueprint reframe disclosure and (b) test_parse_modal_dat_happy_path single-instance fixture — both still scheduled for slice-G retro.
4. **Phase 11 retro §2 (AdvisorContext.extra unread slot) closed** by the `mode_count_target` passthrough at advisor_critique.py:321. Other Phase 11 retro carry-forwards (§3 manifest-version pinning, §4 permissive-status ranges) remain scheduled.
5. **Slice C must NOT touch `_score_modal_specific_axes` or the four named modal weights** without a methodology-doc update and another MINOR bump. The named-axis-keys test (line 90) will surface any silent drift.
