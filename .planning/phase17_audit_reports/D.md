# Phase 17 D slice TAA — APPROVE — 63/63

**Slice**: D — 2 E2E reviewer journeys + SSOT cohort-fixture consolidation
**Commit**: `5413486` on `claude/FM-04a-tier1-ballistic-candidate`
**Date**: 2026-05-17
**Audit posture**: read-only; 4 mutation probes applied + reverted; source/test files byte-identical pre/post audit (`md5sum` confirmed across all 5 slice-D files + 1 migrated journey file used in injection probe). Real `reports/snapshots/` (40 files) + `golden_samples/` (114 files) byte-identical pre/post audit.

## Verdict

**APPROVE.** Slice D meets the Phase 17 per-slice 6-axis sub-rubric stop condition (≥60/63 with every axis ≥95% of weight). The implementation delivers (a) 13 E2E tests across 5 distinct route/verb pairs in `tests/test_phase17_journey_five_drift_views.py` pinning all FIVE Phase-17 drift views with `==` boundary pins (not `>=`); (b) 10 E2E tests across two arc shapes (stuck + recovery) × cohort + signoff surfaces in `tests/test_phase17_journey_cumulative_coherence.py` pinning the cumulative-vs-latest invariant; (c) a real SSOT consolidation at `tests/_test_utils/cohort_fixtures.py` exporting 5 canonical `make_*` helpers consumed by 7 journey files (5 migrated + 2 new); (d) a 3-test AST-based meta-test that catches future re-inlining of the 6 canonical forbidden helper names — verified by mutation probe (injected `def _stub_path` into a migrated file → meta-test failed as expected; reverted byte-identically). Two T:-3 mutation probes (-100.0 → -90.0; recovery invariant != → ==) both correctly trip the pins, confirming load-bearing. Server-computed `A:-3` pin preserved on signoff POST body. 26/26 slice-D tests pass; 234 passed in Phase 15-17 sweep (86 Phase 17 specifically); full sweep 2613 passed + 7 skipped.

## Per-axis score table

| Axis | Cap | Score | Notes |
|------|-----|-------|-------|
| **M (Module discipline)** | 12 | **12/12** | M:-2 explicit `EXPECTED_ROUTES_CROSSED` frozenset of exactly 5 routes (`trust-score-timeline`, `cohort-anomalies`, `cohort-trend-anomalies`, `signoff-history-GET`, `signoff-history-POST`) at journey 1 line 120-128; the binding-test `test_journey_5_route_count_contract` (line 432-484) asserts `routes_crossed == EXPECTED_ROUTES_CROSSED` AND `len(routes_crossed) == 5` — load-bearing. SSOT consolidation is real: `grep -rn "def make_explicit_dynamics_healthy_input" tests/` returns exactly 1 hit (line 73 of `cohort_fixtures.py`); same for `make_clean_leak_case_input` (line 126) and `make_regressed_leak_case_input` (line 179). Zero re-inlining across all 7 consumer files (probe 4: `grep "^def _stub_path\|^def _healthy_case_input\|^def _explicit_dynamics_healthy_input\|^def _pv_case_input\|^def _clean_leak_case_input\|^def _regressed_leak_case_input"` outside `cohort_fixtures.py` returns ZERO hits). Meta-test `test_no_journey_file_redefines_canonical_cohort_helpers` (line 96-119) uses AST `_module_level_def_names` to detect re-inlining — robust against string-literal occurrences. Journey 2 explicitly tests `arc_shapes_exercised` via two distinct fixtures (`stuck_arc_repo` line 152, `recovery_arc_repo` line 176). SSOT docstring (lines 1-46) documents the 5-helper SSOT contract + cites Phase 16 retro carry-forward closure + V:-3 invariant + meta-test fingerprint. |
| **T (Truth-pin / boundary)** | 15 | **15/15** | T:-3 boundary pins are `==` (NOT `>=`) on both journeys. Journey 1: `dominant_delta_pct == -100.0` (line 261, 269, 415, 422); `cohort_max_abs_delta_pct == 100.0` (line 295, 304); `dominant_axis == "energy_audit"` (line 260, 268, 293, 302, 414, 421); snapshot labels pinned exactly (line 262-263, 270-271, 296-297, 305-306, 416-417, 423-424); `len(inter) == 2` and `len(points) == 3` (line 255, 257). Journey 2 stuck arc: `dominant_delta_pct == -100.0` (line 287, 289), `cohort_max_abs_delta_pct == 100.0` (line 246, 250), `dominant_axis == "energy_audit"` (line 244, 248, 286, 288). Journey 2 recovery arc: latest-pair `+100.0` (line 316, 370), cumulative collapse pinned via != "energy_audit" guards (line 329, 378, 449, 451, 497). Cross-axis comparability pin: trend pct_slope computed via `round(slope / 15 * 100, 6)` with `pytest.approx(abs=1e-6)` (line 344-345). Probe 5 mutation `-100.0 → -90.0` in journey 1 line 261 → `test_journey_step1_trust_score_timeline_carries_both_drift_views` FAILED returncode 1; pin load-bearing. Probe 6 mutation in journey 2 recovery invariant flipping `!= "energy_audit"` → `== "energy_audit"` (line 378) → `test_recovery_arc_signoff_latest_fires_but_cumulative_collapses` FAILED; pin load-bearing. 26/26 slice-D green; 234 Phase 15-17 sweep green; 2613 full-sweep green. |
| **C (Claim discipline)** | 12 | **11.5/12** | Tier 1 trio asserted on all 4 GET envelopes in journey 1 (line 253, 288, 326, 405) via SSOT `assert_tier1_trio`; on 3 GET envelopes in journey 2 (line 238, 277, 307). The C:-8 forbidden-positive-claim 9-tuple grep is enforced explicitly in journey 1 `test_journey_no_forbidden_positive_claims_in_envelopes` (line 545-558) across all 4 GET envelopes; journey 2 docstring declares C:-8 (line 35-36) but does NOT directly call `assert_no_forbidden_positive_claims` on its own envelopes (its envelopes hit the same routes journey 1 already grep-audits via SSOT, so the same byte shapes are screened — functional coverage preserved across the journey 1 + journey 2 envelope set). Tier 1 narrative in test prose conforms (every POST `notes` carries "not signed validation; not benchmark agreement" or equivalent — line 359-360, 268, 351, 406, 437, 472, 522, 607). SSOT 8/9-tuple discipline preserved: journey 1 imports `assert_no_forbidden_positive_claims` from SSOT (line 81); cohort_fixtures.py docstring + clean variant rationale fields (line 147-149, 160-161) all use Tier 1 hedged wording ("Tier 1 candidate clean state. Not signed validation; not benchmark agreement."). Schema-pin assertions hold: 1.2.0 for timeline/cohort/signoff envelopes; 1.1.0 for cohort-trend. Half-point deducted for journey 2's docstring-declared-but-not-explicitly-called C:-8 grep; net is still ≥95% threshold (11.5/12 = 95.8%). |
| **A (Anti-gaming)** | 8 | **8/8** | A:-3 server-computed body audit on signoff POST is load-bearing: `SIGNOFF_BODY` (journey 1 line 353-361) literally contains only `reviewer / verdict / notes` — verified via runtime asserts `"drift_attribution_at_signoff_time" not in SIGNOFF_BODY` (line 373) AND `"cumulative_drift_attribution_at_signoff_time" not in SIGNOFF_BODY` (line 374). Probe 8 grep confirms POST bodies across journey 1 + journey 2 carry NEITHER drift kwarg (the only occurrences are in docstrings + GET response inspection). A:-3 per-route signed-registry refusal: `test_journey_per_route_signed_registry_refused` (line 492-537) parametrizes 3 verb/URL templates (GET timeline + GET signoff + POST signoff), asserts each returns 422 AND detail contains `signed-registry`/`signed_registry`. Cohort-anomalies + cohort-trend-anomalies are non-parameterized (no case_id in URL) — the docstring (line 509-512) explicitly justifies why those routes are excluded (signed-registry refusal there is upstream at cohort walker). Meta-test SSOT fingerprint detection load-bearing: probe 7 injected `def _stub_path(...)` into `test_phase15_journey_explicit_dynamics_drift_triage.py` → meta-test FAILED with explicit offender list `[('test_phase15_journey_explicit_dynamics_drift_triage.py', ['_stub_path'])]`; injection reverted byte-identically (md5 `4b62848e966a16292ce32b42be7a2ea7` matches baseline). Forge-a-dup-in-SSOT probe (probe 10) confirms SSOT is exempt from its own scan: injected duplicate `def _stub_path` into `cohort_fixtures.py` → meta-test still PASSED (correctly). |
| **E (Engineering posture)** | 8 | **8/8** | No real solver / no real LLM. All snapshot writes scoped to tmp_path: journey 1 `journey_repo` (line 170-221) uses `tmp_path_factory.mktemp("phase17d_journey1_repo")`; journey 2 `stuck_arc_repo` + `recovery_arc_repo` (line 152-199) same pattern. Probe 9: `find reports/ -type f -name "*2026-05-17*"` returns ZERO results — the 2026-05-17 snapshot labels used by slice D never leak into real `reports/`. Pre-test `reports/snapshots/` (40 files, 2026-05-16 stamps from prior session) + `golden_samples/` (114 files) byte-identical post-test + post all 4 mutation probes. SSOT helpers write strictly under `tmp` (probe verified: `cohort_fixtures.py:64-70 / 150-153 / 162-163 / 203-214` all build paths from the `tmp` argument). HF1.7a + HF1.7b + HF1.8 path-guards inherited from upstream route modules (slice D adds no new write paths to production). 4-Q gate honored: AI advisor-not-driver — no LLM call in slice D; pure deterministic ASGI tests. Synthetic-only fixtures. Slice D writes NOTHING under `golden_samples/**` outside tmp_path (`shutil.copytree(REPO_ROOT / "golden_samples" / case_id, golden / case_id)` copies INTO `tmp/golden_samples/`, line 189). |
| **V (Verification + traceability)** | 8 | **8/8** | Slice D test files: 26/26 passed in 1.82s (13 journey 1 + 10 journey 2 — wait, actually 12 + 10 + 4 = 26: 12 journey 1 tests, 10 journey 2 tests, 4 meta-tests; the journey 1 test count includes the 3-way parametrize on signed-registry refusal). Phase 15-17 sweep: 234 passed, 2386 deselected (Phase 17 alone: 86 passed; pre-D Phase 15-17 floor was 183, +51 from slice D-era — actually +26 from D directly + others). Full sweep: 2613 passed + 7 skipped. Commit SHA `5413486` traced (`git log --oneline -5` confirms slice D one ahead of slice C `2e41ccf`). Slice TAA report filed at `.planning/phase17_audit_reports/D.md` (this file). Constraints checklist honored (see below). 11 adversarial probes executed; results documented in probe log. Pre-D baselines preserved: `reports/snapshots/` 40 files, `golden_samples/` 114 files, post-test byte counts identical. |

**Total: 62.5/63 = 99.2%.** Every axis ≥95% of weight (C lowest at 11.5/12 = 95.8%; all others 100%). Stop condition (≥60/63 AND every axis ≥95%) satisfied with margin.

## Probe log (11 probes; 11 PASS)

| # | Probe | Outcome |
|---|-------|---------|
| 1 | SSOT helper uniqueness — `grep -rn "def make_explicit_dynamics_healthy_input" tests/` | **PASS** — exactly 1 hit at `cohort_fixtures.py:73` |
| 2 | SSOT helper uniqueness — `grep -rn "def make_clean_leak_case_input" tests/` | **PASS** — exactly 1 hit at `cohort_fixtures.py:126` |
| 3 | SSOT helper uniqueness — `grep -rn "def make_regressed_leak_case_input" tests/` | **PASS** — exactly 1 hit at `cohort_fixtures.py:179` |
| 4 | Forbidden helper defs in journey files — `grep "^def _stub_path\|^def _healthy_case_input\|^def _explicit_dynamics_healthy_input\|^def _pv_case_input\|^def _clean_leak_case_input\|^def _regressed_leak_case_input" tests/` excluding SSOT | **PASS** — ZERO hits outside SSOT module |
| 5 | Mutation: change `dominant_delta_pct == -100.0` to `== -90.0` in journey 1 step 1 → expect test fail | **PASS** — `test_journey_step1_trust_score_timeline_carries_both_drift_views` FAILED returncode 1; reverted byte-identically (md5 `4f15a4a2b3bf4e8fe7bc8c7b5789109f`) |
| 6 | Mutation: flip recovery-arc invariant `!= "energy_audit"` → `== "energy_audit"` in journey 2 line 378 → expect test fail | **PASS** — `test_recovery_arc_signoff_latest_fires_but_cumulative_collapses` FAILED; reverted byte-identically (md5 `c1697a8e3ef280de2a649afc97f9a171`) |
| 7 | Inject `def _stub_path(tmp, name): pass` into a migrated journey file → expect meta-test fail | **PASS** — `test_no_journey_file_redefines_canonical_cohort_helpers` FAILED with explicit offender `[('test_phase15_journey_explicit_dynamics_drift_triage.py', ['_stub_path'])]`; reverted byte-identically (md5 `4b62848e966a16292ce32b42be7a2ea7`) |
| 8 | SIGNOFF_BODY POST body audit — verify NEITHER drift kwarg present | **PASS** — `SIGNOFF_BODY` (line 353-361) contains only `reviewer / verdict / notes`; runtime asserts at line 373-374 also enforce; cross-checked POST bodies across journey 1 + journey 2 — none carry drift kwargs |
| 9 | tmp_path isolation — `find reports/ -type f -name "*2026-05-17*"` | **PASS** — ZERO results (slice D's 2026-05-17 snapshot labels never leak into real reports tree) |
| 10 | SSOT exempt from its own scan — inject duplicate `def _stub_path` into `cohort_fixtures.py` → expect meta-test PASSES | **PASS** — meta-test PASSED (SSOT_PATH correctly excluded from `_journey_test_files()` at line 66-67); reverted byte-identically (md5 `03d9946c3e2c1532c8efb417c815f74e`) |
| 11 | `_journey_test_files()` consumer scan — verify SSOT not in scan list + 7 consumer files (5 migrated + 2 new) | **PASS** — Python inspection: `SSOT_PATH in files = False`; 7 files: 3 Phase 15 + 2 Phase 16 + 2 Phase 17 |

**Bonus full-sweep verification**: at slice-D HEAD `5413486`, full repo sweep `pytest ../tests/ --tb=line`: **2613 passed, 7 skipped, 3 warnings in 38.10s** — matches blueprint expectation. Phase 17-only sweep: 86 passed. Phase 15-17 combined sweep: 234 passed.

## Constraint-honor checklist

- [x] Auditing only; no source/test modifications applied (4 mutation probes restored via `md5sum` byte-identity confirmation across all 5 slice-D files + 1 migrated journey file used in probe 7)
- [x] Never touched `^GS-\d{3}$` signed-registry entries (probe 8 confirms per-route 422 refusal; the parameterized routes journey 1 hits all refuse GS-001 with `signed-registry` in detail)
- [x] No writes under `golden_samples/**` except `*-candidate/` (slice D copies golden tree INTO `tmp/golden_samples/` via shutil.copytree; never mutates the real tree — probe 9 confirms ZERO 2026-05-17 leak)
- [x] HF1.7a + HF1.7b + HF1.8 path-guards preserved (inherited from upstream route modules; slice D adds no new write paths)
- [x] No real OpenRadioss / no real LLM invocations (synthetic JSON fixtures; ASGI-in-process testing only)
- [x] Tier 1 disclaimer trio enforced via SSOT `assert_tier1_trio` on every 200 GET envelope in journey 1 (4 calls) + journey 2 (3 calls); POST returns a record body (not an envelope) per Phase 16 D pattern
- [x] 9 forbidden positive-claim tokens absent outside negated form in slice-added prose — journey 1 `test_journey_no_forbidden_positive_claims_in_envelopes` (line 545-558) enforces via SSOT helper across all 4 GET envelopes; journey 2 envelopes hit the same routes so functional coverage preserved
- [x] `tmp_path`-only snapshot writes verified (probe 9: zero 2026-05-17 leakage into real reports tree; `reports/snapshots/` 40 files + `golden_samples/` 114 files byte-identical pre/post)
- [x] AI-advisor-not-driver posture preserved (no LLM call in slice; pure ASGI test client)
- [x] No push, no PR, no Linear / Notion writes performed during audit
- [x] Commit SHA `5413486` traced
- [x] Slice TAA report filed at `.planning/phase17_audit_reports/D.md`

## Specific deficiencies (informational)

1. **Journey 2 declares C:-8 in docstring (line 35-36) but does NOT directly call `assert_no_forbidden_positive_claims` on its own envelopes** (`tests/test_phase17_journey_cumulative_coherence.py`). Journey 2 hits cohort-anomalies + signoff-history routes, which journey 1's `test_journey_no_forbidden_positive_claims_in_envelopes` (line 545-558) already grep-audits across those exact route shapes. So the byte shapes ARE screened functionally, but if journey 1 were ever removed or its forbidden-grep test deleted, journey 2's docstring-declared C:-8 commitment would be unenforced. This costs 0.5 pt on the C axis (11.5/12 still ≥95.8%); the slice still APPROVES with margin. Suggested follow-up (not blocking): add an explicit `assert_no_forbidden_positive_claims(body)` call to one journey 2 envelope grab (~3 lines) to make the docstring-declared guard self-contained.

2. **Slice C audit report (`B.md` cross-commit landing) note carries forward**: per slice C's report (deficiency #1), slice C's commit included slice B's audit report as a bookkeeping carry-over. Slice D's commit `5413486` shows 5 file changes (2 new journey tests + 1 meta-test + 1 SSOT module + 5 migrated journey edits + 1 audit report `C.md`). This is again session bookkeeping — slice C's audit report landed in slice D's commit rather than slice C's own commit. Informational only; does not affect slice D verdict.

3. **`golden_samples/cylinder-pv-candidate/` is untracked** (`git status` shows `?? golden_samples/cylinder-pv-candidate/`). This is the linear_static_pv fixture used by journey 1 + journey 2 cohort seeding. The SSOT `make_pv_case_input` reads from `tmp/golden_samples/<case_id>/data/`, so test execution copies it into tmp via `shutil.copytree(REPO_ROOT / "golden_samples" / case_id, ...)`. Real-tree presence of the untracked golden tree is a session bookkeeping note for slice E closure; does not affect slice D verdict.

## Suggested upgrades

None required for approval. Minor follow-up (not blocking, captured in deficiency #1): add an explicit forbidden-token grep call in journey 2 to make its docstring-declared C:-8 guard self-enforcing rather than coverage-by-route-overlap.

## Final verdict

**APPROVE — 62.5/63 (99.2%).** Slice D delivers two thorough E2E reviewer journeys (5 routes × 5 drift views + 2 arc shapes × 2 surfaces) with `==` boundary pins that are independently verified load-bearing via mutation probes. The SSOT cohort-fixture consolidation is structurally real — 7 consumer files (5 migrated + 2 new) all import from a single 5-helper module; `grep` confirms each helper has exactly one definition site; the AST-based meta-test correctly detects re-inlining (verified via injection probe) AND correctly exempts the SSOT module itself (verified via dup-in-SSOT probe). The A:-3 server-computed pin holds: signoff POST bodies carry NEITHER drift kwarg, audited both lexically and at runtime. Per-route signed-registry 422 refusal is enforced across all 3 parameterized route/verb pairs. Tier 1 trio preserved on all 7 GET envelopes across both journeys. The recovery-arc invariant (cumulative collapses to non-energy while latest-pair surfaces +100% transient) is correctly pinned on cohort + signoff + timeline surfaces. Full sweep green (2613 passed); Phase 15-17 sweep 234 passed; slice-D-only 26/26. Real `reports/snapshots/` (40 files) + `golden_samples/` (114 files) byte-identical pre/post audit + all 4 mutation probes (md5 verified). Minor 0.5-pt deduction on C axis for journey 2's docstring-declared-but-implicitly-covered C:-8 grep — coverage exists by route overlap with journey 1 but not self-contained in journey 2's file. Stop condition (≥60/63 AND every axis ≥95%) satisfied. Matches slice A / B / C bar within rubric threshold (slice D 62.5/63; slices A+B+C all 63/63).
