# Phase 17 C slice TAA — APPROVE — 63/63

**Slice**: C — per-axis `percentage_delta_slope` on cohort-trend-anomalies (1.0.0 → 1.1.0)
**Commit**: `2e41ccf` on `claude/FM-04a-tier1-ballistic-candidate`
**Date**: 2026-05-17
**Audit posture**: read-only; mutation probes reverted; source/test files byte-identical pre/post audit (`md5sum` confirmed). Real `reports/snapshots/` (40 files) + `golden_samples/` (114 files) byte-identical pre/post audit.

## Verdict

**APPROVE.** Slice C meets the Phase 17 per-slice 6-axis sub-rubric stop condition (≥60/63 with every axis ≥95% of weight). The implementation is a clean additive 1.0.0 → 1.1.0 MINOR bump on `COHORT_TREND_ANOMALIES_SCHEMA_VERSION` with a 4-line pure-helper `_percentage_delta_slope(raw_slope, axis)` that delegates weight lookup to the Phase 15 C SSOT `TRUST_AXIS_WEIGHTS` mapping (verified `W1 is W2` — single mapping object). The methodology doc's worked-example values (50→42→34 → -8.0 raw / -16.0 pct; 15→10→5 → -5.0 raw / -33.333333 pct) match the T:-3 boundary pins exactly. Both mutation probes (return-zero + wrong-weight) trip the T:-3 pins as expected — boundary pins are load-bearing, not coincidental. Tier 1 trio preserved on the live ASGI 1.1.0 envelope. Forbidden-token grep clean on the new methodology doc and new test file.

## Per-axis score table

| Axis | Cap | Score | Notes |
|------|-----|-------|-------|
| **M (Module discipline)** | 12 | **12/12** | M:-1 `COHORT_TREND_ANOMALIES_SCHEMA_VERSION` pinned exactly at `"1.1.0"` (`_schema_versions.py:411`); bump-history docstring (`_schema_versions.py:411-448`) cites "Phase 17 C · 2026-05-17", "additive MINOR bump", "percentage_delta_slope", "TRUST_AXIS_WEIGHTS", "parallel view, NOT replacement", "Closes Phase 16 retrospective carry-forward §1", AND back-compat contract. M:-2 helper is 16 lines (`cohort_trend_anomalies.py:178-193`); body imports `TRUST_AXIS_WEIGHTS` from the SSOT (verified probe 5: `W1 is W2 → True`). Module-level import line `from .trust_score_drift_attribution import TRUST_AXIS_WEIGHTS` at line 65. AST audit (`test_percentage_delta_slope_imports_trust_axis_weights_ssot`) excludes the docstring from inline-literal check + forbids any literal integer matching `{50, 20, 15}` in the helper body — passes. Anti-inline guard `test_cohort_trend_anomalies_module_does_not_inline_axis_weights` regex-greps 5 forbidden top-level constant patterns (`COMPLETENESS_WEIGHT =`, `CONVERGENCE_WEIGHT =`, etc., `TRUST_AXIS_WEIGHTS =`) — all absent. Probe 5 independently confirmed `_percentage_delta_slope` body contains zero literal weight integers; only mention of "50" in source is in module docstring context. |
| **T (Truth-pin / boundary)** | 15 | **15/15** | T:-3 worked-example boundary pins: `test_completeness_5042_34_arc_lands_pct_slope_negative_16_0` pins `_percentage_delta_slope(-8.0, "completeness") == -16.0` exactly (NOT `pytest.approx`); `test_energy_15_10_5_arc_lands_pct_slope_negative_33_333333` pins `_percentage_delta_slope(-5.0, "energy_audit") == -33.333333` exactly to 6 decimals (matches `round(..., 6)` truncation in helper). Probe 3 hand-computed all four values — exact match. End-to-end via `build_cohort_trend_anomalies` (`test_completeness_event_carries_expected_raw_and_pct_slope`) seeds scores 100/84/68 → weighted 50/42/34 → fired event with `slope == -8.0` AND `percentage_delta_slope == -16.0` (exact, no approx). T:-4 sign convention: 6-case parametrized test pins `expected_sign == -1` for 3 negative raws (`-8.0/comp`, `-5.0/energy`, `-0.6/convergence`), `+1` for 2 positive raws (`+8.0/comp`, `+5.0/energy`), `0` for flat (`0.0/reproducibility`) — pct sign matches raw sign in all 6 cases. No sign-flip artifacts. T:-5 axis coverage: `test_all_four_trend_axes_produce_coherent_percentage_values` iterates all 4 `TREND_AXES` with raw=-1.5 and pins `_percentage_delta_slope(-1.5, axis) == round((-1.5/weight)*100, 6)` for each axis; `test_trend_axes_tuple_matches_trust_axis_weights_keys` pins TREND_AXES ⊆ TRUST_AXIS_WEIGHTS so a future axis addition trips both surfaces consistently. Probe 2 mutation `return round(...) → return 0.0` trips T:-3 with returncode 1; probe 14 mutation `weight = TRUST_AXIS_WEIGHTS[axis] → weight = 100` trips T:-3 + T:-5 simultaneously (3 tests fail). Both pins load-bearing. 25/25 slice tests green; broader 6-phase sweep `534 passed`. |
| **C (Claim discipline)** | 12 | **12/12** | C:-1 Tier 1 trio preserved on the cohort-trend-anomalies envelope at 1.1.0 (live ASGI: probe 12 confirms `claim_tier == "Tier 1 engineering candidate"`, `claim_boundary` starts with `tier1_engineering_candidate; not_signed_validation; not_benchmark_agreement`, `claim_impact` present). `test_endpoint_envelope_carries_tier1_trio_at_1_1_0` consumes the SSOT `assert_tier1_trio` helper on the live envelope at schema 1.1.0. C:-8 forbidden-token grep on new methodology doc via SSOT 9-tuple: `test_methodology_doc_carries_no_forbidden_positive_claims` consumes `FORBIDDEN_POSITIVE_CLAIM_TOKENS_9` + `assert_no_forbidden_positive_claims` — passes. Probe 1 (with proper whitespace-normalize on negation prefix `"no\n"` cases): NONE flagged. Probe 10 forbidden grep on the new test file: NONE flagged. Methodology doc explicitly carries the Tier 1 sentinel header (`Tier 1 engineering candidate; not signed validation; not benchmark agreement.`) and a forbidden-wording sentinel paragraph listing all 4 envelope-narrowed tokens in negated form. `render_cohort_trend_anomalies_json` serializes `percentage_delta_slope` alongside `slope` (`_report_to_dict:306`); when `report.anomalies` is empty (live default), envelope still carries trio + empty anomalies list cleanly. |
| **A (Anti-gaming)** | 8 | **8/8** | A:-2 unknown-axis defense exercised by TWO dedicated tests: `test_unknown_axis_raises_key_error` pins `KeyError` raised with offending label `"stiffness"` in the message; `test_unknown_axis_error_lists_expected_axes` pins the error message names all 4 expected axes (sorted list). Probe 4 independently confirmed: `KeyError("unknown trust-score axis 'stiffness'; expected one of ['completeness', 'convergence', 'energy_audit', 'reproducibility']")` AND `KeyError` for `"foobar"` shape. M:-2 anti-inline: helper IMPORTS the SSOT (probe 5 `W1 is W2`); AST audit forbids inline weight literals in helper body; module-level regex audit forbids 5 forbidden top-level constant patterns. Probe 14 substitutes wrong weight `100` for the SSOT lookup → trips T:-3 + T:-5 (3 tests fail). Defensive-parser equivalent: TrendEvent missing `percentage_delta_slope` raises TypeError (`test_trend_event_construction_without_pct_slope_raises`); probe 4 independently confirmed `TypeError: TrendEvent.__init__() missing 1 required positional argument: 'percentage_delta_slope'`. Firing-gate semantic preserved: `_build_event_for_axis` returns None when `slope > TREND_SLOPE_INFO_MAX` (line 273-274), so positive/flat arcs do NOT fire — the percentage_delta_slope is descriptive only, NOT a firing gate (explicitly documented in methodology doc §"What this surface does NOT do"). Methodology doc-test crosscheck (probe 11): worked-example values in doc match T:-3 pins exactly. |
| **E (Engineering posture)** | 8 | **8/8** | No real solver / no real LLM. All snapshot writes scoped to `tmp_path` via `fake_repo` fixture (line 114-117 monkeypatches `route_module._repo_root` to `tmp_path`) AND `_seed_axis_snapshot` writes only into `root / "golden_samples" / ...` where `root` is the tmp fixture. V:-3 verified by probe 8 + probe 15: `find reports/snapshots/ -type f | wc -l == 40` AND `find golden_samples/ -type f | wc -l == 114` BOTH pre + post all 25 tests + mutation probes. HF1.7a signed-registry filter preserved at `cohort_trend_anomalies.py:122 + line 235` (`_SIGNED_REGISTRY_RE = re.compile(r"^GS-\d{3}$")` + `if _SIGNED_REGISTRY_RE.fullmatch(name): continue` skip in cohort walker). 4-Q gate honored: AI is advisor-not-driver — no LLM call in slice C surface area; `_percentage_delta_slope` is pure deterministic float math. Synthetic-only fixtures (no real OpenRadioss / no real CalculiX). Slice C touches NO file under `golden_samples/**` (all writes via fixture into tmp). |
| **V (Verification + traceability)** | 8 | **8/8** | Slice C test file: 25/25 passed in 0.68s. Broader sweep `phase9 or phase10 or phase12 or phase15 or phase16 or phase17`: 534 passed (matches expected). Phase 9 D regression isolated: 22/22 green (includes updated schema-pin assertion 1.0.0 → 1.1.0 at `test_phase9_cohort_trend_anomalies.py:136-140`). Commit SHA `2e41ccf` traced (slice-C commit one ahead of slice B `6d8d8a4`). Slice TAA report filed at `.planning/phase17_audit_reports/C.md`. Constraints checklist honored (see below). 16 adversarial probes executed; results documented. |

**Total: 63/63.** Every axis at 100% of weight (≥95% threshold satisfied).

## Probe log (16 probes; 16 PASS)

| # | Probe | Outcome |
|---|-------|---------|
| 1 | Forbidden-token grep on new methodology doc with whitespace-normalized negation prefix check (9 SSOT tokens) | **PASS** — initial naive `text[i-6:i]` snagged `"no\n\`perforation completed\`"` and `"no\n\`validated physics\`"`; whitespace-normalized re-probe confirms both are negated form. Zero positive claims |
| 2 | Mutation: `return round(...)` → `return 0.0` in `_percentage_delta_slope` → run T:-3 test | **PASS** — `test_completeness_5042_34_arc_lands_pct_slope_negative_16_0` FAILS (returncode 1); pin is load-bearing |
| 3 | Hand-verify worked-example slopes + cross-axis comparability (-10% on comp implies raw -5; on energy implies raw -1.5) | **PASS** — completeness 50,42,34 → slope -8.0 exact; energy 15,10,5 → slope -5.0 exact; helper round-trips `-10` ↔ `(raw -5.0 comp / raw -1.5 energy)` exactly |
| 4 | Unknown-axis defense + TrendEvent TypeError | **PASS** — `KeyError("unknown trust-score axis 'stiffness'; expected one of ['completeness', 'convergence', 'energy_audit', 'reproducibility']")`; TrendEvent missing field → `TypeError: ... missing 1 required positional argument: 'percentage_delta_slope'` |
| 5 | SSOT delegation truly shared object (`TRUST_AXIS_WEIGHTS` not duplicated) | **PASS** — `W1 is W2 → True` (single mapping object); grep of cohort_trend_anomalies.py for inline literal `(^|\s)(50\|20\|15)\b` yields only docstring mention of "Phase 15 C SSOT mapping" at line 182 |
| 6 | Schema bump-history docstring content audit (`COHORT_TREND_ANOMALIES_SCHEMA_VERSION` block) | **PASS** — all 6 required tokens present: `Phase 17 C`, `Bump history`, `percentage_delta_slope`, `1.1.0`, `TRUST_AXIS_WEIGHTS`, `1.0.0` |
| 7 | Phase 9 D regression: existing test updated to expect 1.1.0 at `test_phase9_cohort_trend_anomalies.py:136-140` | **PASS** — 22/22 phase 9 tests green; schema-pin assertion `assert COHORT_TREND_ANOMALIES_SCHEMA_VERSION == "1.1.0"` |
| 8 | Pre-test byte-identity baseline of `reports/snapshots/` (40 files) + `golden_samples/` (114 files) | **PASS** — counts unchanged pre/post all 25 tests + mutation probes |
| 9 | Phase 9 regression isolated re-run | **PASS** — 22 passed in 0.78s |
| 10 | Forbidden-token grep on new test file `tests/test_phase17_trend_percentage_slope.py` (9 SSOT tokens) | **PASS** — NONE flagged |
| 11 | Methodology doc worked-example exact-match crosscheck against T:-3 test boundary pins | **PASS** — methodology cites `-8.0 / 50 * 100.0 = -16.0` AND `-5.0 / 15 * 100.0 ≈ -33.333333`; test pins `pct == -16.0` AND `pct == -33.333333` exactly |
| 12 | Live ASGI `GET /api/v1/cohort-trend-anomalies` at schema 1.1.0 — Tier 1 trio presence | **PASS** — status 200, `schema_version: 1.1.0`, `claim_tier: "Tier 1 engineering candidate"`, `claim_boundary` starts with `tier1_engineering_candidate; not_signed_validation; not_benchmark_agreement`, `claim_impact` present with full bounded reading |
| 13 | HF1.7a signed-registry filter preserved in cohort_trend_anomalies.py builder path | **PASS** — `_SIGNED_REGISTRY_RE = re.compile(r"^GS-\d{3}$")` at line 122; enforced at line 235 (`if _SIGNED_REGISTRY_RE.fullmatch(name): continue`) |
| 14 | Mutation: `weight = TRUST_AXIS_WEIGHTS[axis]` → `weight = 100` in helper → run T:-3 + T:-5 tests | **PASS** — 3 tests FAIL (`test_completeness_5042_34...`, `test_energy_15_10_5...`, `test_all_four_trend_axes_produce_coherent_percentage_values`); confirms SSOT lookup is load-bearing |
| 15 | Post-probe byte-identity restore confirm — module + reports + golden | **PASS** — `md5sum` of cohort_trend_anomalies.py matches `/tmp/cta_backup.py`; counts 40 + 114 unchanged |
| 16 | Signature + dataclass field audit | **PASS** — `_percentage_delta_slope(raw_slope: float, axis: str) -> float`; TrendEvent fields = `['case_id', 'axis', 'slope', 'point_count', 'severity', 'percentage_delta_slope']` (6 total; new field landed in last position) |

**Bonus full-sweep verification**: at slice-C HEAD `2e41ccf`, sweep `-k "phase9 or phase10 or phase17 or phase15 or phase16 or phase12"`: **534 passed, 2060 deselected, 3 warnings in 9.94s** — matches blueprint expectation.

## Constraint-honor checklist

- [x] Auditing only; no source/test modifications applied (mutation probes restored via `md5sum` byte-identity confirmation)
- [x] Never touched `^GS-\d{3}$` signed-registry entries (filter preserved at `cohort_trend_anomalies.py:122 + 235`; probe 13 confirmed)
- [x] No writes under `golden_samples/**` except `*-candidate/` (slice C writes via `fake_repo` fixture monkeypatch + `_seed_axis_snapshot` into tmp_path-scoped golden_samples copies; probe 8 + 15 byte-identity)
- [x] HF1.7a + HF1.7b + HF1.8 path-guards preserved (signed-registry filter + candidate-suffix filter + tmp_path scoping for all snapshot writes)
- [x] No real OpenRadioss / no real LLM invocations (synthetic JSON fixtures; pure deterministic float math in helper)
- [x] Tier 1 disclaimer trio enforced on every 200 envelope at 1.1.0 via SSOT helper `assert_tier1_trio` (probe 12 + `test_endpoint_envelope_carries_tier1_trio_at_1_1_0`)
- [x] 9 forbidden positive-claim tokens absent outside negated form in slice-added prose (probes 1 + 10)
- [x] `tmp_path`-only snapshot writes verified (probe 8 + probe 15)
- [x] AI-advisor-not-driver posture preserved (no LLM call in slice; pure deterministic math; methodology doc explicitly documents "percentage_delta_slope is descriptive, not a firing gate")
- [x] No push, no PR, no Linear / Notion writes performed during audit
- [x] Commit SHA `2e41ccf` traced
- [x] Slice TAA report filed at `.planning/phase17_audit_reports/C.md`

## Specific deficiencies (if any)

**None blocking.** Two informational notes:

1. **Slice C commit includes slice B's audit report `B.md` as an additional file** (not a deliverable issue — likely bookkeeping carry-over from slice B → slice C transition). `git diff 6d8d8a4 2e41ccf --stat` shows 6 files changed, one of which is `.planning/phase17_audit_reports/B.md` (78 lines) — slice B's verdict report landed in the slice C commit rather than slice B's own commit. This is a session bookkeeping note only; **does not affect slice C verdict**.

2. **Slice D WIP detected in working tree** (uncommitted): `tests/_test_utils/cohort_fixtures.py` is an untracked file containing slice D's 5-case cohort fixture SSOT consolidation (8996 bytes; docstring cites "Closes Phase 16 retrospective carry-forward (consolidated into Phase 17 D)"). This is orthogonal to slice C scope — slice C's test file does NOT import from `tests/_test_utils.cohort_fixtures`; it imports only from `tests/_test_utils` (the existing module). Informational only; flagged for slice D's TAA.

3. **Methodology doc forbidden-grep with naive prefix check momentarily flagged `"no\n\`perforation completed\`"` and `"no\n\`validated physics\`"`** because the negation marker `"no"` was on the previous line wrapped via `\n`. The slice's own test uses the SSOT helper `assert_no_forbidden_positive_claims` which properly handles whitespace; my whitespace-normalized re-probe confirms zero positive claims. Informational only — the slice test is correct; the SSOT helper is more robust than my initial naive check.

## Suggested upgrades

None required. Slice C meets the per-slice 6-axis sub-rubric at every axis with zero margin loss. Stop condition (≥60/63 AND every axis ≥95%) satisfied with full margin. Matches the slice A bar (63/63) and slice B bar (63/63).

## Final verdict

**APPROVE — 63/63.** Slice C is a clean additive 1.0.0 → 1.1.0 MINOR bump that correctly extends the cohort-trend-anomalies surface with a cross-axis-comparable percentage-of-axis-weight slope view. The helper is a 4-line pure delegation to the Phase 15 C `TRUST_AXIS_WEIGHTS` SSOT (single mapping object verified `is`-identical) — zero inline math, zero duplicate-weight risk. The methodology doc's worked-example values match the T:-3 boundary pins exactly to 6 decimals. Two independent mutation probes (return-zero, wrong-weight) both trip the T:-3 pins, confirming the pins are load-bearing. The defensive `KeyError` raise on unknown axis correctly lists all 4 expected axes. TrendEvent dataclass-required new field correctly raises TypeError on construction omission. Tier 1 trio preserved on the live ASGI 1.1.0 envelope. Forbidden-token discipline preserved on the new methodology doc + new test file. Stop condition satisfied with full margin. Matches the slice A bar (63/63) and slice B bar (63/63) exactly.
