## TAA report — Phase 7 B @ 23fb6b6

### Axis verdicts
- **B (schema versioning, weight 12): FLAG** — `SNAPSHOT_NARRATIVE_SCHEMA_VERSION` stays at `"1.0.0"` (`backend/app/services/reporting/_schema_versions.py:188`) while `_narrative_to_dict` now emits a new top-level `"locale"` key on every body (`snapshot_narrative.py:452`). The author's own commit-message label "additive-within-MINOR pattern, same as the diff numerical_deltas in Phase 6 A" is self-contradicting: Phase 6 A *did* MINOR-bump for additive (`4018671 Phase6-A: snapshot diff raw-value extension + MINOR schema bump`). Phase 7 A precedent (manifest 1.1.0→1.2.0 for additive `convergence_study.json` capture) reinforces the same rule. A 1.0.0→1.1.0 bump on `SNAPSHOT_NARRATIVE_SCHEMA_VERSION` plus a bump-history line is required. Self-attestation 11/12 is overstated.
- **M (module quality, weight 12): APPROVE** — `CATALOGS` is a plain `dict[str, dict[str, str]]` (`snapshot_narrative_catalogs.py:42`); the only `if locale ==` is the import-time consistency audit's `if locale == DEFAULT_LOCALE: continue` (line 170), which skips comparison against self — not a runtime dispatch branch. Every emitter routes through `render_template(template_id, locale, slots)`. Anti-gaming guard M:-3 not triggered.
- **T (testing depth, weight 15): APPROVE** — 34 new tests; 16 per-template zh-CN positives confirmed by grep (`test_zh_cohort_added` ... `test_zh_script_sha_changed`); plus 4 structural + 4 audit + 4 render + 3 envelope + 3 parity. Existing `tests/test_snapshot_narrative.py` untouched (verified via `git diff`); local re-run = 56 passed (22 existing en-US + 34 new). One minor weakness: `test_module_import_runs_forbidden_audit` calls `_audit_catalog_forbidden_claims()` directly rather than via `importlib.reload`, so the *import-time* invocation site itself is only indirectly exercised by `test_shipped_catalog_passes_forbidden_audit`. Not a defect — load-bearing call at `snapshot_narrative_catalogs.py:198-199` is real.
- **C (claim-tier discipline, weight 12): APPROVE** — `_FORBIDDEN_TOKENS` contains all 6 required tokens (4 Phase 6 + 2 Phase 7: `signed validation`, `benchmark agreement`) at lines 128-136. `_audit_catalog_forbidden_claims()` invoked at module scope (line 199, *not* inside a function body, *not* under `if __name__`). Grep of catalog text bodies confirms no forbidden token appears outside the forbidden-list / docstring itself. zh-CN audit fires per `test_zh_cn_catalog_forbidden_claim_audit_fires`. HF1 zone untouched.
- **X (frontend integration, weight 12): FLAG** — `frontend/src/snapshotNarrativeClient.ts` exports `SUPPORTED_NARRATIVE_LOCALES`, `DEFAULT_NARRATIVE_LOCALE`, type `NarrativeLocale`; `SnapshotNarrative.locale` field plumbed; `fetchSnapshotNarrative` accepts optional `locale` parameter (lines 22-24, 36, 109, 128). No vitest/headless smoke (carry-forward §2 still open). Self-attestation 3/12 is honest; no rubric block, just deferred.
- **D (documentation / SSOT, weight 8): FLAG** — Catalog module docstring documents slot-conventions (`snapshot_narrative_catalogs.py:21-31`) and route docstring documents `?locale=` (`snapshot_narrative.py:48-52` in routes). However, **`_schema_versions.py` bump-history was NOT updated** because no bump was made — coupled to axis B FLAG. If B is corrected, D requires a bump-history line documenting the 1.0.0→1.1.0 reason ("locale field added to envelope").
- **A (anti-gaming discipline, weight 8): APPROVE** — Catalog drift audit + forbidden-claim audit are two independent guards triggered by negative tests (`test_template_id_drift_audit_fires`, `test_zh_cn_catalog_forbidden_claim_audit_fires`). Slice A TAA APPROVE already in repo as `.planning/phase7_audit_reports/A.md`.
- **E (end-to-end workflow, weight 8): FLAG** — No new E2E in slice B (locale roundtrip via real HTTP endpoint not exercised in this commit's tests). Blueprint §3.B targets E2E in a later slice. Honest 0/8 in commit message.
- **V (verification independence, weight 13): FLAG** — Slice A TAA report archived. This (slice B) TAA report is being written now; without push/PR review the independence rubric only scores partial. No defect; just unfinished surface.

### Defects
- **HIGH** — `backend/app/services/reporting/_schema_versions.py:188` — `SNAPSHOT_NARRATIVE_SCHEMA_VERSION` left at `"1.0.0"` despite envelope-level `locale` field addition. **Fix:** bump to `"1.1.0"` and add a bump-history entry citing Phase 7 B + the locale envelope addition. Consumer parsers that whitelist envelope keys would see a silent contract change.
- **MEDIUM** — `backend/app/services/reporting/snapshot_narrative.py:459-464` — runtime `_assert_no_overclaim` lists 4 forbidden tokens; catalog audit `_FORBIDDEN_TOKENS` (`snapshot_narrative_catalogs.py:128-136`) lists 6. The two lists are now duplicated and can drift. **Fix:** import `_FORBIDDEN_TOKENS` from the catalogs module (or extract to `_schema_versions.py` / a shared constants module) and reuse. No correctness gap today since catalog audit catches the extra two at import time, but two sources of truth = drift risk.
- **LOW** — `tests/test_phase7_locale_narrative_catalog.py:96` — `test_module_import_runs_forbidden_audit` calls `cat_mod._audit_catalog_forbidden_claims()` directly inside a try/finally; the test name implies an `importlib.reload`-driven fire-on-import. **Fix:** rename to `test_audit_forbidden_claims_raises_on_injected_token` or add a sibling test that does `importlib.reload(cat_mod)` after corrupting `CATALOGS` to verify the import-time call site actually fires. Cosmetic; the audit is genuinely invoked at module scope.

### Carry-forward closure status
- **§3 narrative templates English-only: CLOSED** — en-US + zh-CN catalogs ship; endpoint accepts `?locale=`; 16 templates × 2 locales = 32 fixed translations with no LLM call; per-template zh-CN positive coverage; locale field round-trips through HTTP body. Evidence: `snapshot_narrative_catalogs.py:42-122` (catalogs), `snapshot_narrative.py:96-167` (locale-parametrized builder), `routes/snapshot_narrative.py:40-62` (whitelist + 400). The §3 footnote "No bump-policy implication; the JSON contract stays stable" turned out to be inaccurate once the implementation chose to surface `locale` on the envelope rather than purely input-only — see axis B FLAG.

### Overall verdict
**CHANGES_REQUIRED** — One HIGH defect (schema bump missing) plus one MEDIUM (duplicated forbidden token list). Both are surgical fixes (≤10 LOC each + 1 test + 1 bump-history line) and do not invalidate the catalog architecture, which is sound.

### Honest score adjustment relative to the commit's claimed 66/100
| Axis | Claimed | Recommended | Delta | Reason |
|------|---------|-------------|-------|--------|
| B    | 11/12   | **7/12**    | **-4** | Envelope-level `locale` addition without `SNAPSHOT_NARRATIVE_SCHEMA_VERSION` bump contradicts the Phase 6 A + Phase 7 A precedent and the explicit anti-gaming rubric pattern (additive ⇒ MINOR bump + bump-history). Author's "additive-within-MINOR" label is internally inconsistent with not having bumped. Bump-history also missing once B is fixed (would also affect D). |
| M    | 12/12   | 12/12       |  0    | Dict dispatch is genuine; the `== DEFAULT_LOCALE` is an audit-loop guard, not a runtime dispatch. |
| T    | 11/15   | 11/15       |  0    | Honest; carry-forward on property-based + headless frontend acknowledged. |
| C    | 12/12   | **11/12**   | **-1** | Forbidden-token list duplicated between `snapshot_narrative.py` runtime check and `snapshot_narrative_catalogs.py` import-time check — drift risk; not a current correctness gap but a soft anti-gaming weakness. |
| X    | 3/12    | 3/12        |  0    | Honest. |
| D    | 7/8     | **6/8**     | **-1** | `_schema_versions.py` bump-history not updated (couples to B). |
| A    | 8/8     | 8/8         |  0    | Honest. |
| E    | 0/8     | 0/8         |  0    | Honest. |
| V    | 2/13    | 2/13        |  0    | This report adds to V once archived. |
| **Total** | **66/100** | **60/100** | **-6** | Slice B is structurally sound but two real defects (B HIGH, C MEDIUM) need fix-up commits before slice C inherits the contract. |
