"""AI-FEA tool drivers.

Thin wrappers around external binaries (FreeCAD, Gmsh, CalculiX)
and file parsers (.frd).  Each driver exposes a functional API
that agents call via LangGraph tool-call actions.
"""
