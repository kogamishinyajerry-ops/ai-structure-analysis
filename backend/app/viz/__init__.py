"""Result-viewer artifact pipeline (ADR-016).

This package owns the `.frd → .vtu` writer (Phase 2.2 follow-up) and the
`runs/{id}/viz/manifest.json` artifact format. The manifest schema lives
in `schemas.viz_manifest`. See `docs/adr/ADR-016-frd-vtu-result-viz.md`.
"""
