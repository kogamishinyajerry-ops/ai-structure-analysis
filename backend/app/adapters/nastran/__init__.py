"""Nastran (.op2 / .bdf) adapter — placeholder.

RFC-001 §4.5 work item: wrap ``pyNastran`` (BSD, 10y mature) as a
Layer-1 ``ReaderHandle``. Lands in W3 per RFC §6.4 — placed early
because pyNastran is the most mature open-source reader and validates
the Layer-2 contract before ANSYS lands.
"""
