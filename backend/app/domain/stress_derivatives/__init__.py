"""Stress-tensor derived quantities — placeholder.

RFC-001 §4.2 + ADR-001: von Mises, principal stresses, max-shear, and
SCL (Stress Classification Line for ASME pressure vessels) live here,
not in any adapter. Lands in W3 alongside the second adapter so the
"derive-once, use-everywhere" path is exercised across at least two
solvers before W4 freezes the API.
"""
