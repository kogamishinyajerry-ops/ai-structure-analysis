"""Agentic FEA Workflow Runtime — M1 mock substrate.

The staged, observable pipeline runtime (plan `.planning/AGENTIC-FEA-RUNTIME-PLAN.md`).
M1 ships a Mock pipeline (synthetic solver data, real `StageState` events) so the
flow + Monitor UI are provable before Trigger.dev (M2) and the real solver (M4).
"""
